# Công Pháp 2.0 (Phase 2) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Thêm trục progression cho công pháp: tier 1–3 + ba nhánh (Tu Luyện/Chiến Đạo/Đan Đạo), 9 môn tier 2 học bằng Bí Tịch + Linh Thạch, hai buff hệ thống mới (linhKhiRate nhân tốc độ tu luyện, danDaoSuccess cộng % luyện đan) nối vào hook sẵn có từ Phase 1.

**Architecture:** Mở rộng additive: 4 cột mới trên `CongPhap` + `RedeemCodeReward.materialId`; union key `EffectAttribute` trong `attributes.calc.ts` (tách hàm `sumSystemBuffs`, không đổi `computeAttributes`); học môn qua `POST /congphap/:id/learn` chạy Serializable tx theo pattern `levelUpWithCosts`; buff tiêu thụ ở các call-site `computeLinhKhi`/`computeSuccessPct`.

**Tech Stack:** Express + Prisma (PostgreSQL), Vitest (unit + integration vs DB docker `backend-db-1`), Next.js frontend, Biome.

**Spec:** `docs/superpowers/specs/2026-08-07-cong-phap-2-design.md` (nguồn chân lý cho mọi con số/tên). Kiểm tra cuối task: `cd backend && npm test` (≥448), `cd frontend && pnpm test && pnpm lint && pnpm exec tsc --noEmit` (≥130).

---

### Task 1: Prisma schema + migration + repo mapping

**Files:**
- Modify: `backend/prisma/schema.prisma` (model `CongPhap` ~dòng 161, `Material` ~dòng 201, `RedeemCodeReward` ~dòng 137)
- Modify: `backend/src/infrastructure/repositories/PrismaCongPhapRepository.ts` (hàm `toRecord` dòng 7–23)
- Modify: `backend/src/infrastructure/repositories/PrismaOwnedCongPhapRepository.ts` (hàm `toEntry` dòng 6–29)
- Modify: `backend/src/infrastructure/repositories/PrismaRedeemCodeRepository.ts` (mapping rewards, nếu có)
- Create: `backend/prisma/migrations/<timestamp>_cong_phap_2/migration.sql`

- [ ] **Step 1: Sửa schema**

Thêm vào `CongPhap` (sau `cooldownRounds`):

```prisma
  tier             Int       @default(1)
  branch           String?   // "tuLuyen" | "chienDao" | "danDao"; null = không thuộc nhánh
  minRealmMajor    Int       @default(0)
  biTichMaterialId String?
  biTichMaterial   Material? @relation("CongPhapBiTichMaterial", fields: [biTichMaterialId], references: [id])

  @@index([tier, branch])
```

Thêm vào `Material`:

```prisma
  biTichCongPhaps CongPhap[] @relation("CongPhapBiTichMaterial")
  redeemRewards    RedeemCodeReward[]
```

Thêm vào `RedeemCodeReward` (giữ nguyên ba cột cũ):

```prisma
  materialId String?
  material   Material? @relation(fields: [materialId], references: [id])
```

- [ ] **Step 2: Tạo migration additive (KHÔNG `migrate dev` — dev DB có drift `TowerProgress`)**

```bash
cd backend && npx prisma migrate diff --from-migrations prisma/migrations --to-schema-datamodel prisma/prisma/schema.prisma --script
```

(Kiểm tra đúng path schema: nếu `prisma/schema.prisma` thì dùng đường dẫn đó.) Tạo thư mục `prisma/migrations/$(date +%Y%m%d%H%M%S)_cong_phap_2/migration.sql` chứa script, thêm phần `ALTER TABLE` và cuối file thêm backfill:

```sql
UPDATE "CongPhap" SET branch = 'chienDao' WHERE id IN ('thiet-cot-quyet', 'linh-tuc-quyet', 'liet-hoa-tam');
```

Áp vào dev DB + đánh dấu applied:

```bash
docker exec -i backend-db-1 psql -U postgres -d tvt_game < prisma/migrations/<ts>_cong_phap_2/migration.sql
cd backend && npx prisma migrate resolve --applied <ts>_cong_phap_2 && npx prisma generate
```

Expected: `prisma migrate status` sạch; `npx prisma generate` OK.

- [ ] **Step 3: Cập nhật mapping repo (compile pass, chưa đổi hành vi)**

`PrismaCongPhapRepository.toRecord` — mở rộng type của tham số `row` thêm `tier: number; branch: string | null; minRealmMajor: number; biTichMaterialId: string | null` và return spread (đã `...row`). Đồng thời domain record ở Task 3 mới có field — tạm cast:

```ts
return {
  ...row,
  category: row.category as CongPhapCategory,
  branch: (row.branch as CongPhapBranch | null) ?? null,
  effects: (row.effects as PassiveEffect[] | null) ?? null,
  // ...giữ các dòng cũ
};
```

`CongPhapBranch` import từ `../../domain/congphap/congphap` — nếu Task 3 chưa chạy, định nghĩa type này NGAY trong Task 1 (đặt `export type CongPhapBranch = 'tuLuyen' | 'chienDao' | 'danDao';` đầu file `domain/congphap/congphap.ts`) để compile; Task 3 chỉ thêm field vào interface. Làm tương tự cho `toEntry` trong `PrismaOwnedCongPhapRepository.ts`. Redeem repo: thêm `materialId: row.materialId ?? null` vào mapping rewards (nếu file có mapping thủ công; nếu trả thẳng row Prisma thì không cần).

- [ ] **Step 4: Verify compile + test hiện hữu**

Run: `cd backend && npm run build && npm test`
Expected: build sạch; toàn bộ test cũ vẫn pass (domain field mới optional-chưa-bắt-buộc ở bước này nhờ default ở schema và mapping `?? null`).

- [ ] **Step 5: Commit**

```bash
git add backend/prisma backend/src/infrastructure/repositories backend/src/domain/congphap/congphap.ts
git commit -m "feat: schema cong phap 2 (tier/branch/realm gate/bi tich + redeem material reward)"
```

---

### Task 2: Union key EffectAttribute + sumSystemBuffs

**Files:**
- Modify: `backend/src/domain/attributes/attributes.calc.ts`
- Test: `backend/src/domain/attributes/attributes.calc.test.ts` (tạo nếu chưa có)

- [ ] **Step 1: Viết test fail**

```ts
import { describe, it, expect } from 'vitest';
import { computeAttributes, sumSystemBuffs } from './attributes.calc';

const BASE = { khiHuyet: 10, chanNguyen: 0, congVatLy: 0, congPhep: 0, phongThu: 0, tocDo: 0 };

describe('sumSystemBuffs', () => {
  it('gom pctPerLevel × level của 2 key đặc biệt qua nhiều môn', () => {
    const buffs = sumSystemBuffs([
      { level: 3, effects: [{ attribute: 'linhKhiRate', flatPerLevel: 0, pctPerLevel: 2 }] },
      { level: 2, effects: [{ attribute: 'danDaoSuccess', flatPerLevel: 0, pctPerLevel: 1.5 }] },
    ]);
    expect(buffs.linhKhiRatePct).toBe(6);
    expect(buffs.danDaoSuccessPct).toBe(3);
  });

  it('bỏ qua key attribute thường', () => {
    expect(sumSystemBuffs([{ level: 5, effects: [{ attribute: 'khiHuyet', flatPerLevel: 10, pctPerLevel: 2 }] }]))
      .toEqual({ linhKhiRatePct: 0, danDaoSuccessPct: 0 });
  });
});

describe('computeAttributes với key đặc biệt', () => {
  it('không crash và bỏ qua key đặc biệt', () => {
    const { final } = computeAttributes(BASE, [
      { level: 2, effects: [
        { attribute: 'linhKhiRate', flatPerLevel: 0, pctPerLevel: 5 },
        { attribute: 'khiHuyet', flatPerLevel: 10, pctPerLevel: 0 },
      ] },
    ]);
    expect(final.khiHuyet).toBe(30); // 10 base + 20 flat; linhKhiRate bị lờ
  });
});
```

- [ ] **Step 2: Chạy test — FAIL** (`sumSystemBuffs` chưa tồn tại; computeAttributes crash với key lạ)

Run: `cd backend && npx vitest run src/domain/attributes/attributes.calc.test.ts`

- [ ] **Step 3: Implement**

Trong `attributes.calc.ts`:

```ts
export type EffectAttribute = keyof AttributeSet | 'linhKhiRate' | 'danDaoSuccess';

export interface PassiveEffect {
  attribute: EffectAttribute;
  flatPerLevel: number;
  pctPerLevel: number;
}

export interface SystemBuffs {
  linhKhiRatePct: number;   // % cộng dồn, tiêu thụ: rate × (1 + pct/100)
  danDaoSuccessPct: number; // điểm % cộng vào computeSuccessPct (clamp 5..95 có sẵn)
}

export function sumSystemBuffs(passives: OwnedPassive[]): SystemBuffs {
  let linhKhiRatePct = 0;
  let danDaoSuccessPct = 0;
  for (const p of passives) {
    for (const e of p.effects) {
      if (e.attribute === 'linhKhiRate') linhKhiRatePct += e.pctPerLevel * p.level;
      else if (e.attribute === 'danDaoSuccess') danDaoSuccessPct += e.pctPerLevel * p.level;
    }
  }
  return { linhKhiRatePct, danDaoSuccessPct };
}
```

Sửa vòng lặp trong `computeAttributes` bỏ qua key không thuộc `AttributeSet`:

```ts
for (const p of passives) {
  for (const e of p.effects) {
    if (!ATTRIBUTE_KEYS.includes(e.attribute as keyof AttributeSet)) continue;
    const attr = e.attribute as keyof AttributeSet;
    flat[attr] += e.flatPerLevel * p.level;
    pct[attr] += e.pctPerLevel * p.level;
  }
}
```

- [ ] **Step 4: Test pass + full suite xanh**

Run: `cd backend && npx vitest run src/domain/attributes && npm test`
Expected: test mới pass, mọi test cũ giữ nguyên.

- [ ] **Step 5: Commit**

```bash
git add backend/src/domain/attributes
git commit -m "feat: union EffectAttribute + sumSystemBuffs (linhKhiRate, danDaoSuccess)"
```

---

### Task 3: CongPhapRecord fields + validate rules mới

**Files:**
- Modify: `backend/src/domain/congphap/congphap.ts`
- Modify: `backend/src/domain/congphap/congphap.validate.ts`
- Test: `backend/src/domain/congphap/congphap.validate.test.ts`

- [ ] **Step 1: Viết test fail** (thêm case vào `congphap.validate.test.ts`)

```ts
const baseDef = (): CongPhapRecord => ({
  id: 'test-cong', name: 'Test', glyph: '测', rarity: 1, category: 'passive', desc: 'x',
  active: true, maxLevel: 10, baseCost: 100, costGrowth: 1.5,
  effects: [{ attribute: 'khiHuyet', flatPerLevel: 10, pctPerLevel: 0 }],
  powerPerLevel: null, chanNguyenCost: null, dupRefundLinhThach: null,
  upgradeMaterialId: null, baseMaterialCost: 0, materialCostGrowth: 1, cooldownRounds: null,
  tier: 1, branch: 'chienDao', minRealmMajor: 0, biTichMaterialId: null,
});

it('chấp nhận key đặc biệt với flat=0 pct>0', () => {
  expect(() => validateCongPhapDefinition({
    ...baseDef(), effects: [{ attribute: 'danDaoSuccess', flatPerLevel: 0, pctPerLevel: 1 }],
  })).not.toThrow();
});
it('chặn key đặc biệt có flatPerLevel ≠ 0', () => {
  expect(() => validateCongPhapDefinition({
    ...baseDef(), effects: [{ attribute: 'linhKhiRate', flatPerLevel: 1, pctPerLevel: 2 }],
  })).toThrowDomainError('INVALID_CONGPHAP_CONFIG');
});
it('chặn tier ≥ 2 thiếu branch hoặc biTich', () => {
  expect(() => validateCongPhapDefinition({ ...baseDef(), tier: 2, branch: null })).toThrowDomainError('INVALID_CONGPHAP_CONFIG');
  expect(() => validateCongPhapDefinition({ ...baseDef(), tier: 2, biTichMaterialId: null })).toThrowDomainError('INVALID_CONGPHAP_CONFIG');
});
it('chặn tier ngoài 1..3 / minRealmMajor âm', () => {
  expect(() => validateCongPhapDefinition({ ...baseDef(), tier: 4 })).toThrowDomainError('INVALID_CONGPHAP_CONFIG');
  expect(() => validateCongPhapDefinition({ ...baseDef(), minRealmMajor: -1 })).toThrowDomainError('INVALID_CONGPHAP_CONFIG');
});
```

(`toThrowDomainError` là matcher hiện có của suite; nếu không có thì dùng pattern expect-throw cũ của file.)

- [ ] **Step 2: Chạy test — FAIL**

Run: `cd backend && npx vitest run src/domain/congphap/congphap.validate.test.ts`

- [ ] **Step 3: Implement**

`congphap.ts` — interface `CongPhapRecord` thêm:

```ts
  tier: number;                       // 1..3 (bảng tier chung master spec)
  branch: CongPhapBranch | null;      // null = không thuộc nhánh (môn tier 1 legacy)
  minRealmMajor: number;              // 0 = không gate; tier 2 → 3 (Kết Đan), tier 3 → 5 (Hóa Thần)
  biTichMaterialId: string | null;    // null = môn chỉ học qua redeem
```

`congphap.validate.ts` — sửa mảng key và thêm rule (sau các check cũ):

```ts
const SYSTEM_EFFECT_KEYS = ['linhKhiRate', 'danDaoSuccess'] as const;

// trong nhánh passive, thay dòng check attribute:
for (const e of def.effects) {
  const isSystemKey = (SYSTEM_EFFECT_KEYS as readonly string[]).includes(e.attribute);
  const isAttributeKey = ATTRIBUTE_KEYS.includes(e.attribute as keyof AttributeSet);
  if (!isSystemKey && !isAttributeKey) fail(`unknown attribute "${e.attribute}"`);
  if (!Number.isFinite(e.flatPerLevel) || !Number.isFinite(e.pctPerLevel)) fail('effect values must be finite numbers');
  if (isSystemKey && (e.flatPerLevel !== 0 || e.pctPerLevel <= 0)) {
    fail(`system effect "${e.attribute}" requires flatPerLevel = 0 and pctPerLevel > 0`);
  }
}

// cuối hàm:
if (!Number.isInteger(def.tier) || def.tier < 1 || def.tier > 3) fail('tier must be an integer in 1..3');
if (def.branch !== null && !['tuLuyen', 'chienDao', 'danDao'].includes(def.branch)) fail(`unknown branch "${def.branch}"`);
if (!Number.isInteger(def.minRealmMajor) || def.minRealmMajor < 0) fail('minRealmMajor must be an integer >= 0');
if (def.tier >= 2) {
  if (def.branch === null) fail('tier >= 2 requires a branch');
  if (def.biTichMaterialId === null) fail('tier >= 2 requires biTichMaterialId');
}
```

- [ ] **Step 4: Test pass + full suite** (sửa fixture record cũ trong các test/model hiện hữu thêm 4 field từ `baseDef` nếu compile kêu: seed không — seed dùng TS literal, xử ở Task 10; ở đây chỉ files domain/tests/admin.catalog)

Run: `cd backend && npm run build && npm test`

- [ ] **Step 5: Commit**

```bash
git add backend/src/domain/congphap backend/tests 2>/dev/null; git add -A backend/src
git commit -m "feat: congphap record + validate tier/branch/realm gate/system effect keys"
```

---

### Task 4: learnGate domain (tối giản)

**Files:**
- Modify: `backend/src/domain/congphap/congphap.calc.ts`
- Test: `backend/src/domain/congphap/congphap.calc.test.ts`

- [ ] **Step 1: Viết test fail**

```ts
import { learnGate, LEARN_LINH_THACH_COST } from './congphap.calc';

it('môn không có biTich → not-learnable', () => {
  expect(learnGate({ ...baseDef(), biTichMaterialId: null }, 5)).toEqual({ ok: false, reason: 'not-learnable' });
});
it('realm thấp hơn minRealmMajor → realm-gate', () => {
  expect(learnGate({ ...baseDef(), biTichMaterialId: 'bi-tich-x', minRealmMajor: 3 }, 2))
    .toEqual({ ok: false, reason: 'realm-gate' });
});
it('đủ điều kiện → ok; LEARN_LINH_THACH_COST = 300', () => {
  expect(learnGate({ ...baseDef(), biTichMaterialId: 'bi-tich-x', minRealmMajor: 3 }, 3)).toEqual({ ok: true });
  expect(LEARN_LINH_THACH_COST).toBe(300);
});
```

- [ ] **Step 2: FAIL** → **Step 3: Implement** trong `congphap.calc.ts`:

```ts
// Học môn qua Bí Tịch: giá Linh Thạch phẳng theo spec Phase 2 (mọi môn tier 2 = 300).
export const LEARN_LINH_THACH_COST = 300;

export type LearnGateResult = { ok: true } | { ok: false; reason: 'not-learnable' | 'realm-gate' };

export function learnGate(def: CongPhapRecord, realmMajor: number): LearnGateResult {
  if (def.biTichMaterialId === null) return { ok: false, reason: 'not-learnable' };
  if (realmMajor < def.minRealmMajor) return { ok: false, reason: 'realm-gate' };
  return { ok: true };
}
```

- [ ] **Step 4: pass** — Run: `cd backend && npx vitest run src/domain/congphap`
- [ ] **Step 5: Commit** `git add backend/src/domain/congphap && git commit -m "feat: learnGate + LEARN_LINH_THACH_COST"`

---

### Task 5: ProgressionRepository.learnWithCosts + integration test

**Files:**
- Modify: `backend/src/domain/ports/ProgressionRepository.ts`
- Modify: `backend/src/infrastructure/repositories/PrismaProgressionRepository.ts`
- Test: `backend/src/infrastructure/repositories/congphap.repo.integration.test.ts` (thêm describe mới)

Điều kiện: DB test đang chạy (`docker ps | grep backend-db-1`).

- [ ] **Step 1: Port type**

Trong `ProgressionRepository.ts`:

```ts
export type LearnWithCostsResult =
  | { kind: 'learned'; linhThach: number; biTichQuantity: number }
  | { kind: 'missing-bitich' }
  | { kind: 'insufficient-linh-thach' }
  | { kind: 'already-owned' }
  | { kind: 'concurrent' };

export interface ProgressionRepository {
  // ...giữ levelUpWithCosts
  // Học môn bằng 1 Bí Tịch + linhThachCost trong MỘT transaction Serializable.
  // Thứ tự: tạo OwnedCongPhap (P2002 -> already-owned), trừ Bí Tịch, trừ Linh Thạch.
  learnWithCosts(input: {
    userId: string;
    congPhapId: string;
    biTichMaterialId: string;
    linhThachCost: number;
  }): Promise<LearnWithCostsResult>;
}
```

- [ ] **Step 2: Viết integration test fail** (mô phỏng describe `levelUpWithCosts` hiện có; dùng user/video fixture của file)

```ts
describe('learnWithCosts', () => {
  it('happy path: trừ 1 Bí Tịch + 300 LT + tạo owned level 1', async () => { /* arrange user có 2 bi-tich + 500 LT; expect kind learned, biTichQuantity 1, linhThach 200; owned tồn tại */ });
  it('thiếu Bí Tịch → missing-bitich, không ghi gì', async () => { /* expect kind; inventory/linhThach/owned không đổi */ });
  it('đủ Bí Tịch thiếu LT → insufficient-linh-thach, Bí Tịch KHÔNG bị trừ', async () => { /* expect kind + đọc lại bi-tich quantity giữ nguyên */ });
  it('đã sở hữu → already-owned (P2002), không trừ gì', async () => { /* grant trước rồi learn; expect kind; biTich quantity giữ nguyên */ });
  it('hai request đồng thời → đúng 1 learned, cái còn lại already-owned hoặc concurrent', async () => {
    const [a, b] = await Promise.all([learn(...), learn(...)]);
    const kinds = [a.kind, b.kind].sort();
    expect(kinds).toEqual(['learned', expect.stringMatching(/already-owned|concurrent/)].sort());
  });
});
```

- [ ] **Step 3: FAIL** (`learnWithCosts` chưa có)

Run: `cd backend && npx vitest run src/infrastructure/repositories/congphap.repo.integration.test.ts`

- [ ] **Step 4: Implement** trong `PrismaProgressionRepository.ts` (thêm signal kết quả, mở rộng `ProgressionSignal` cho `LearnWithCostsResult` khác 'learned'):

```ts
async learnWithCosts(input: {
  userId: string; congPhapId: string; biTichMaterialId: string; linhThachCost: number;
}): Promise<LearnWithCostsResult> {
  try {
    return await this.client.$transaction(async (tx) => {
      try {
        await tx.ownedCongPhap.create({ data: { userId: input.userId, congPhapId: input.congPhapId } });
      } catch (e) {
        if (e instanceof Prisma.PrismaClientKnownRequestError && e.code === 'P2002') {
          throw new ProgressionSignal({ kind: 'already-owned' });
        }
        throw e;
      }
      const material = await tx.materialInventory.updateMany({
        where: { userId: input.userId, materialId: input.biTichMaterialId, quantity: { gte: 1 } },
        data: { quantity: { decrement: 1 } },
      });
      if (material.count !== 1) throw new ProgressionSignal({ kind: 'missing-bitich' });

      const character = await tx.character.updateMany({
        where: { userId: input.userId, linhThach: { gte: input.linhThachCost } },
        data: { linhThach: { decrement: input.linhThachCost } },
      });
      if (character.count !== 1) throw new ProgressionSignal({ kind: 'insufficient-linh-thach' });

      const [updatedCharacter, biTich] = await Promise.all([
        tx.character.findUniqueOrThrow({ where: { userId: input.userId } }),
        tx.materialInventory.findUnique({ where: { userId_materialId: { userId: input.userId, materialId: input.biTichMaterialId } } }),
      ]);
      return { kind: 'learned' as const, linhThach: updatedCharacter.linhThach, biTichQuantity: biTich?.quantity ?? 0 };
    }, { isolationLevel: Prisma.TransactionIsolationLevel.Serializable });
  } catch (error) {
    if (error instanceof ProgressionSignal) return error.result;
    if (error instanceof Prisma.PrismaClientKnownRequestError && error.code === 'P2034') {
      return { kind: 'concurrent' };
    }
    throw error;
  }
}
```

Lưu ý: `ProgressionSignal` hiện generic theo `LevelUpWithCostsResult` — mở rộng union để chứa cả hai nhóm kind (`'missing-bitich' | 'insufficient-linh-thach' | 'already-owned' | 'concurrent' | 'insufficient-materials'`).

- [ ] **Step 5: Integration test pass + full suite**

Run: `cd backend && npx vitest run src/infrastructure/repositories/congphap.repo.integration.test.ts && npm test`

- [ ] **Step 6: Commit** `git add backend/src && git commit -m "feat: learnWithCosts serializable tx (bi tich + linh thach -> owned)"`

---

### Task 6: LearnCongPhapUseCase + unit test

**Files:**
- Create: `backend/src/application/LearnCongPhapUseCase.ts`
- Test: `backend/src/application/congphap.usecases.test.ts` (thêm describe `LearnCongPhapUseCase`)

- [ ] **Step 1: Test fail**

```ts
describe('LearnCongPhapUseCase', () => {
  it('môn không tồn tại/inactive → CONGPHAP_NOT_FOUND');
  it('môn không có biTich → CONGPHAP_NOT_LEARNABLE');
  it('realm thấp → CONGPHAP_REALM_GATE với message chứa tên cảnh giới');
  it('repo kinds → đúng DomainError: missing-bitich→CONGPHAP_MISSING_BITICH, insufficient-linh-thach→INSUFFICIENT_LINH_THACH, already-owned→CONGPHAP_ALREADY_OWNED, concurrent→CONCURRENT_MODIFICATION');
  it('learned → trả về { owned: id, linhThach, biTich: {id, quantity} } và gọi repo đúng tham số');
});
```

- [ ] **Step 2: FAIL** → **Step 3: Implement**

```ts
import { CongPhapRepository } from '../domain/ports/CongPhapRepository';
import { ProgressionRepository } from '../domain/ports/ProgressionRepository';
import { CharacterRepository } from '../domain/ports/CharacterRepository';
import { RealmConfigSource } from '../domain/ports/RealmConfigSource';
import { DomainError } from '../domain/errors';
import { learnGate, LEARN_LINH_THACH_COST } from '../domain/congphap/congphap.calc';

export class LearnCongPhapUseCase {
  constructor(
    private readonly congphap: CongPhapRepository,
    private readonly progression: ProgressionRepository,
    private readonly characters: CharacterRepository,
    private readonly realmConfig: RealmConfigSource,
  ) {}

  async execute(userId: string, congPhapId: string) {
    const def = await this.congphap.findById(congPhapId);
    if (!def || !def.active) throw new DomainError('CONGPHAP_NOT_FOUND', 'Công pháp không tồn tại');
    const character = await this.characters.findByUserId(userId);
    if (!character) throw new DomainError('CHARACTER_NOT_FOUND', 'Character not found');

    const gate = learnGate(def, character.realmMajor);
    if (!gate.ok && gate.reason === 'not-learnable') {
      throw new DomainError('CONGPHAP_NOT_LEARNABLE', 'Môn này chỉ nhận được qua nguồn khác');
    }
    if (!gate.ok) {
      const realmName = this.realmConfig.get().realmName(def.minRealmMajor);
      throw new DomainError('CONGPHAP_REALM_GATE', `Cần đạt cảnh giới ${realmName}`);
    }

    const result = await this.progression.learnWithCosts({
      userId, congPhapId, biTichMaterialId: def.biTichMaterialId as string, linhThachCost: LEARN_LINH_THACH_COST,
    });
    if (result.kind === 'missing-bitich') throw new DomainError('CONGPHAP_MISSING_BITICH', 'Thiếu Bí Tịch của môn công pháp');
    if (result.kind === 'insufficient-linh-thach') throw new DomainError('INSUFFICIENT_LINH_THACH', 'Không đủ Linh Thạch');
    if (result.kind === 'already-owned') throw new DomainError('CONGPHAP_ALREADY_OWNED', 'Bạn đã sở hữu công pháp này');
    if (result.kind === 'concurrent') throw new DomainError('CONCURRENT_MODIFICATION', 'Thao tác bị xung đột, thử lại');

    return { owned: def.id, linhThach: result.linhThach, biTich: { id: def.biTichMaterialId, quantity: result.biTichQuantity } };
  }
}
```

Realm name lấy qua `realmConfig.get().realmName(major)` — `RealmConfigSource` hiện có (xem AttemptBreakthroughUseCase dùng). Message `Cần đạt cảnh giới <tên>` để FE dùng lại cho hint.

- [ ] **Step 4: Pass + suite** `npx vitest run src/application/congphap.usecases.test.ts && npm test`

- [ ] **Step 5: Commit** `git add backend/src && git commit -m "feat: LearnCongPhapUseCase"`

---

### Task 7: Route POST /congphap/:id/learn + error mapping + wiring

**Files:**
- Modify: `backend/src/presentation/schemas/congphap.schemas.ts`
- Modify: `backend/src/presentation/routes/congphap.routes.ts`
- Modify: `backend/src/presentation/middleware/errorHandler.ts`
- Modify: `backend/src/app.ts` (~dòng 157–173: wiring use case + router deps)

- [ ] **Step 1: Schema** — `congphap.schemas.ts` thêm:

```ts
export const learnSchema = z.object({}); // body rỗng; id từ path param với regex slug ở route
```

(Nếu file đang export equipSchema/unequipSchema/levelUpSchema — thêm cùng style.)

- [ ] **Step 2: Route** — `congphap.routes.ts` thêm vào interface deps `learnCongPhapUseCase: LearnCongPhapUseCase;` và cuối router (TRƯỚC `return router`):

```ts
router.post('/:id/learn', async (req: AuthedRequest, res, next) => {
  try {
    const id = req.params.id;
    if (!/^[a-z0-9-]+$/.test(id)) throw new DomainError('INVALID_INPUT', 'invalid cong phap id');
    res.status(200).json(await deps.learnCongPhapUseCase.execute(req.userId as string, id));
  } catch (err) {
    next(err);
  }
});
```

Route `/:id/learn` không xung đột `/equip|/unequip|/levelup` ("equip" không khớp `/:id/learn` vì thiếu segment thứ hai).

- [ ] **Step 3: Error mapping** — `errorHandler.ts` STATUS_BY_CODE thêm:

```ts
CONGPHAP_NOT_LEARNABLE: 400,
CONGPHAP_REALM_GATE: 409,
CONGPHAP_MISSING_BITICH: 409,
CONGPHAP_ALREADY_OWNED: 409,
```

- [ ] **Step 4: Wiring** — `app.ts`:

```ts
const learnCongPhapUseCase = new LearnCongPhapUseCase(congPhapRepository, progressionRepository, characterRepository, realmConfigProvider);
```

và truyền vào `createCongPhapRouter({ ..., learnCongPhapUseCase })`.

- [ ] **Step 5: Build + test + smoke thủ công**

Run: `cd backend && npm run build && npm test`, sau đó (server dev đang chạy) `curl -X POST :5000/congphap/<id-tier-2>/learn -H "Authorization: Bearer <token dev>"` expect 404/409 hợp lý (không crash route) — hoặc tin API integration test Task 9, ghi chú kết quả.

- [ ] **Step 6: Commit** `git add backend/src && git commit -m "feat: POST /congphap/:id/learn + error mapping"`

---

### Task 8: Wire buff — cultivation rate + alchemy success/duration preview

**Files:**
- Modify: `backend/src/application/attributeState.ts`
- Modify: `backend/src/application/GetCultivationStateUseCase.ts` (2 call-site computeLinhKhi ~dòng 84, 115)
- Modify: `backend/src/application/ConsumePillUseCase.ts` (2 call-site ~dòng 67, 118)
- Modify: `backend/src/application/AttemptBreakthroughUseCase.ts` (call-site ~dòng 49; constructor + app.ts wiring)
- Modify: `backend/src/application/ListAlchemyRecipesUseCase.ts`
- Modify: `backend/src/infrastructure/repositories/PrismaAlchemyRepository.ts` (`settleInTransaction`)
- Modify: `backend/src/app.ts`
- Test: `backend/src/application/attributeState.test.ts`, `backend/src/application/materials.alchemy.usecases.test.ts` (ListAlchemyRecipes), `backend/tests/integration/…` alchemy settle nếu có file tương ứng

- [ ] **Step 1: attributeState helper + test**

```ts
import { sumSystemBuffs, SystemBuffs } from '../domain/attributes/attributes.calc';

// Buff hệ thống từ passive ĐANG SỞ HỮU (cùng filter với buildAttributeState):
// chỉ passive + def.active. Trả multiplier thay vì % để call-site nhân trực tiếp.
export function buildSystemBuffs(owned: OwnedCongPhapEntry[]): SystemBuffs & { linhKhiRateMultiplier: number } {
  const passives = owned
    .filter((o) => o.def.category === 'passive' && o.def.active && o.def.effects)
    .map((o) => ({ level: o.level, effects: o.def.effects! }));
  const buffs = sumSystemBuffs(passives);
  return { ...buffs, linhKhiRateMultiplier: 1 + buffs.linhKhiRatePct / 100 };
}
```

Test: owned gồm `vong-coc-quyet` lv 10 (linhKhiRate 2/lv) + 1 môn inactive có linhKhiRate → multiplier phải là 1.2 (inactive bị lờ).

- [ ] **Step 2: Wire cultivation (TDD trên use-case tests hiện có — thêm case môn Tu Luyện)**

Trong cả 3 use case, tại MỌI call-site `computeLinhKhi({ ..., cultivationRate: stage.cultivationRate, ... })` thay bằng `stage.cultivationRate * linhKhiRateMultiplier`:
- `GetCultivationStateUseCase`: dời `const owned = await this.ownedCongPhap.listByUser(userId)` (hiện ở dòng ~101) lên TRƯỚC call-site computeLinhKhi đầu tiên (~dòng 84) và dùng chung; `const { linhKhiRateMultiplier } = buildSystemBuffs(owned);`.
- `ConsumePillUseCase`: owned đã load sau — dời lên trước call-site đầu (~dòng 67) tương tự.
- `AttemptBreakthroughUseCase`: chưa inject owned — thêm `OwnedCongPhapRepository` vào constructor (tham số cuối), `const owned = await this.ownedCongPhap.listByUser(userId)` trước computeLinhKhi (~dòng 40), cập nhật `app.ts` dòng 157 thêm `ownedCongPhapRepository` vào `new AttemptBreakthroughUseCase(...)`.

Pill buff (`buff.multiplier`) vẫn truyền nguyên — nhân chồng độc lập theo spec.

- [ ] **Step 3: ListAlchemyRecipesUseCase preview**

Constructor thêm `private readonly ownedCongPhap: OwnedCongPhapRepository`; `executeForUser` load owned song song với profile, `const { danDaoSuccessPct } = buildSystemBuffs(owned);` và truyền `danDaoPct: danDaoSuccessPct` vào `computeSuccessPct`. Cập nhật wiring `app.ts`. Test: fixture user sở hữu môn danDao → `effectiveSuccessPct` tăng đúng số điểm.

- [ ] **Step 4: Settle tx**

Trong `settleInTransaction` (`PrismaAlchemyRepository.ts`), sau khi load `profile`, thêm:

```ts
const ownedRows = await tx.ownedCongPhap.findMany({ where: { userId }, include: { congPhap: true } });
const danDaoPct = sumSystemBuffs(
  ownedRows
    .filter((r) => r.congPhap.category === 'passive' && r.congPhap.active && r.congPhap.effects)
    .map((r) => ({ level: r.level, effects: r.congPhap.effects as unknown as PassiveEffect[] })),
).danDaoSuccessPct;
```

và truyền `danDaoPct` vào `settleAlchemyQueue({ ..., danDaoPct })`. (import `sumSystemBuffs`, `PassiveEffect`.) Integration test: user có môn `danDao` lv 10 + recipe base < 100 + random cố định → kết quả settle khớp pct đã cộng.

- [ ] **Step 5: Full suite + build**

Run: `cd backend && npm test && npm run build`

- [ ] **Step 6: Commit** `git add backend/src && git commit -m "feat: tieu thu buff tu luyen (rate) + dan dao (success) vao cultivation/alchemy"`

---

### Task 9: ListCongPhapUseCase enrichment (biTichOwned + system)

**Files:**
- Modify: `backend/src/application/ListCongPhapUseCase.ts`
- Test: `backend/src/application/congphap.usecases.test.ts`

- [ ] **Step 1: Test fail**

```ts
it('catalog entry gắn biTichOwned từ MaterialInventory; response có system buffs', async () => {
  // materials: [{ materialId: 'bi-tich-x', quantity: 2 }]
  // catalog: [{ id,'biTichMaterialId':'bi-tich-x' , tier:2,...}, { biTichMaterialId: null }]
  // owned: [{ dieu-hoa lv10 }]
  expect(result.catalog[0].biTichOwned).toBe(2);
  expect(result.catalog[1].biTichOwned).toBe(0);
  expect(result.system).toEqual({ linhKhiRatePct: 0, danDaoSuccessPct: 10 });
});
```

- [ ] **Step 2: FAIL** → **Step 3: Implement**

```ts
export class ListCongPhapUseCase {
  constructor(
    private readonly owned: OwnedCongPhapRepository,
    private readonly congphap: CongPhapRepository,
    private readonly materials: MaterialRepository,
  ) {}

  async execute(userId: string) {
    const [owned, catalog, inventory] = await Promise.all([
      this.owned.listByUser(userId), this.congphap.listActive(), this.materials.listInventory(userId),
    ]);
    const qtyByMaterial = new Map(inventory.map((i) => [i.materialId, i.quantity]));
    const system = buildSystemBuffs(owned);
    const { linhKhiRateMultiplier: _ignored, ...systemOut } = system;
    return {
      owned,
      catalog: catalog.map((c) => ({ ...c, biTichOwned: c.biTichMaterialId ? (qtyByMaterial.get(c.biTichMaterialId) ?? 0) : 0 })),
      system: systemOut,
    };
  }
}
```

Cập nhật `app.ts` dòng 170 thêm `materialRepository`.

- [ ] **Step 4: Pass + suite** → **Step 5: Commit** `git commit -m "feat: list cong phap tra biTichOwned + system buffs"`

---

### Task 10: Seed tier 2 (9 môn + 9 Bí Tịch + 2 linh tài)

**Files:**
- Modify: `backend/prisma/seed.ts`

- [ ] **Step 1: Gán branch cho 3 môn cũ trong mảng `CONG_PHAP`** — thêm vào mỗi entry: `tier: 1, branch: 'chienDao', minRealmMajor: 0, biTichMaterialId: null`. (Seed upsert sẽ đè — đúng semantic reset catalog.)

- [ ] **Step 2: Thêm seed** — theo ĐÚNG bảng §6 spec (không tự đổi số):

```ts
const CONG_PHAP_TIER2 = [
  { id: 'vong-coc-quyet', name: 'Vong Cốc Quyết', glyph: '忘', rarity: 3, category: 'passive', desc: 'Quên cả cốc thần, tâm ý quy nhất, đẩy nhanh hấp thu linh khí.', active: true, maxLevel: 10, baseCost: 300, costGrowth: 1.6, effects: [{ attribute: 'linhKhiRate', flatPerLevel: 0, pctPerLevel: 2 }], powerPerLevel: null, chanNguyenCost: null, dupRefundLinhThach: null, upgradeMaterialId: 'linh-tai-tu-luyen', baseMaterialCost: 3, materialCostGrowth: 1.6, cooldownRounds: null, tier: 2, branch: 'tuLuyen', minRealmMajor: 3, biTichMaterialId: 'bi-tich-vong-coc' },
  { id: 'tieu-chu-thien-cong', name: 'Tiểu Chu Thiên Công', glyph: '周', rarity: 3, category: 'passive', desc: 'Vận khí theo chu thiên nhỏ, linh khí tuần hoàn nhanh.', active: true, maxLevel: 10, baseCost: 400, costGrowth: 1.6, effects: [{ attribute: 'linhKhiRate', flatPerLevel: 0, pctPerLevel: 3 }], powerPerLevel: null, chanNguyenCost: null, dupRefundLinhThach: null, upgradeMaterialId: 'linh-tai-tu-luyen', baseMaterialCost: 3, materialCostGrowth: 1.6, cooldownRounds: null, tier: 2, branch: 'tuLuyen', minRealmMajor: 3, biTichMaterialId: 'bi-tich-tieu-chu-thien' },
  { id: 'dai-chu-thien-cong', name: 'Đại Chu Thiên Công', glyph: '天', rarity: 4, category: 'passive', desc: 'Chu thiên đại thành, linh khí tự hội tụ không ngừng.', active: true, maxLevel: 10, baseCost: 500, costGrowth: 1.6, effects: [{ attribute: 'linhKhiRate', flatPerLevel: 0, pctPerLevel: 5 }], powerPerLevel: null, chanNguyenCost: null, dupRefundLinhThach: null, upgradeMaterialId: 'linh-tai-tu-luyen', baseMaterialCost: 4, materialCostGrowth: 1.6, cooldownRounds: null, tier: 2, branch: 'tuLuyen', minRealmMajor: 3, biTichMaterialId: 'bi-tich-dai-chu-thien' },
  { id: 'kim-cang-the', name: 'Kim Cang Thể', glyph: '金', rarity: 3, category: 'passive', desc: 'Thân như kim cương bất hoại, khí huyết dồi dào.', active: true, maxLevel: 10, baseCost: 350, costGrowth: 1.6, effects: [{ attribute: 'khiHuyet', flatPerLevel: 200, pctPerLevel: 0 }, { attribute: 'phongThu', flatPerLevel: 0, pctPerLevel: 5 }], powerPerLevel: null, chanNguyenCost: null, dupRefundLinhThach: null, upgradeMaterialId: 'linh-tai-khi-huyet', baseMaterialCost: 3, materialCostGrowth: 1.6, cooldownRounds: null, tier: 2, branch: 'chienDao', minRealmMajor: 3, biTichMaterialId: 'bi-tich-kim-cang' },
  { id: 'ngu-kiem-thuat', name: 'Ngự Kiếm Thuật', glyph: '剑', rarity: 3, category: 'passive', desc: 'Lấy ý ngự kiếm, kiếm khí tăng uy thế vật công.', active: true, maxLevel: 10, baseCost: 400, costGrowth: 1.6, effects: [{ attribute: 'congVatLy', flatPerLevel: 0, pctPerLevel: 8 }], powerPerLevel: null, chanNguyenCost: null, dupRefundLinhThach: null, upgradeMaterialId: 'linh-tai-hoa-luc', baseMaterialCost: 3, materialCostGrowth: 1.6, cooldownRounds: null, tier: 2, branch: 'chienDao', minRealmMajor: 3, biTichMaterialId: 'bi-tich-ngu-kiem' },
  { id: 'thien-loi-chi', name: 'Thiên Lôi Chỉ', glyph: '雷', rarity: 4, category: 'active', desc: 'Một chỉ dẫn lôi thiên, phá vỡ vạn quân.', active: true, maxLevel: 10, baseCost: 450, costGrowth: 1.6, effects: null, powerPerLevel: 250, chanNguyenCost: 40, dupRefundLinhThach: null, upgradeMaterialId: 'linh-tai-hoa-luc', baseMaterialCost: 4, materialCostGrowth: 1.6, cooldownRounds: 3, tier: 2, branch: 'chienDao', minRealmMajor: 3, biTichMaterialId: 'bi-tich-thien-loi' },
  { id: 'dieu-hoa-tan-quyet', name: 'Điều Hỏa Tán Quyết', glyph: '炭', rarity: 3, category: 'passive', desc: 'Điều khiển hỏa lò nhuần nhuyễn, đan thành êm ả.', active: true, maxLevel: 10, baseCost: 300, costGrowth: 1.6, effects: [{ attribute: 'danDaoSuccess', flatPerLevel: 0, pctPerLevel: 1 }], powerPerLevel: null, chanNguyenCost: null, dupRefundLinhThach: null, upgradeMaterialId: 'linh-tai-dan-dao', baseMaterialCost: 3, materialCostGrowth: 1.6, cooldownRounds: null, tier: 2, branch: 'danDao', minRealmMajor: 3, biTichMaterialId: 'bi-tich-dieu-hoa' },
  { id: 'ninh-dan-kinh', name: 'Ngưng Đan Kinh', glyph: '凝', rarity: 3, category: 'passive', desc: 'Kinh văn ngưng luyện đan tinh, giảm hỏa hầu thất thường.', active: true, maxLevel: 10, baseCost: 400, costGrowth: 1.6, effects: [{ attribute: 'danDaoSuccess', flatPerLevel: 0, pctPerLevel: 1.5 }], powerPerLevel: null, chanNguyenCost: null, dupRefundLinhThach: null, upgradeMaterialId: 'linh-tai-dan-dao', baseMaterialCost: 3, materialCostGrowth: 1.6, cooldownRounds: null, tier: 2, branch: 'danDao', minRealmMajor: 3, biTichMaterialId: 'bi-tich-ninh-dan' },
  { id: 'van-linh-lo-quyet', name: 'Vạn Linh Lô Quyết', glyph: '炉', rarity: 4, category: 'passive', desc: 'Lô quyết vạn linh, đan đạo thông thiên.', active: true, maxLevel: 10, baseCost: 500, costGrowth: 1.6, effects: [{ attribute: 'danDaoSuccess', flatPerLevel: 0, pctPerLevel: 2 }], powerPerLevel: null, chanNguyenCost: null, dupRefundLinhThach: null, upgradeMaterialId: 'linh-tai-dan-dao', baseMaterialCost: 4, materialCostGrowth: 1.6, cooldownRounds: null, tier: 2, branch: 'danDao', minRealmMajor: 3, biTichMaterialId: 'bi-tich-van-linh-lo' },
];

const MATERIALS_TIER2_CONGPHAP = [
  { id: 'linh-tai-tu-luyen', name: 'Linh Tài Tu Luyện', glyph: '氣', rarity: 3, description: 'Linh tài chuyên dùng để tu tập tâm pháp vận khí.', tier: 2 },
  { id: 'linh-tai-dan-dao', name: 'Linh Tài Đan Đạo', glyph: '丹', rarity: 3, description: 'Linh tài chuyên dùng để tu tập đan đạo tâm pháp.', tier: 2 },
  // 9 Bí Tịch — mỗi quyển ứng đúng một môn tier 2 ở trên
  { id: 'bi-tich-vong-coc', name: 'Bí Tịch: Vong Cốc Quyết', glyph: '秘', rarity: 4, description: 'Bí tịch cổ ghi lại Vong Cốc Quyết; dùng để nhập môn.', tier: 2 },
  { id: 'bi-tich-tieu-chu-thien', name: 'Bí Tịch: Tiểu Chu Thiên Công', glyph: '秘', rarity: 4, description: 'Bí tịch cổ ghi lại Tiểu Chu Thiên Công; dùng để nhập môn.', tier: 2 },
  { id: 'bi-tich-dai-chu-thien', name: 'Bí Tịch: Đại Chu Thiên Công', glyph: '秘', rarity: 4, description: 'Bí tịch cổ ghi lại Đại Chu Thiên Công; dùng để nhập môn.', tier: 2 },
  { id: 'bi-tich-kim-cang', name: 'Bí Tịch: Kim Cang Thể', glyph: '秘', rarity: 4, description: 'Bí tịch cổ ghi lại Kim Cang Thể; dùng để nhập môn.', tier: 2 },
  { id: 'bi-tich-ngu-kiem', name: 'Bí Tịch: Ngự Kiếm Thuật', glyph: '秘', rarity: 4, description: 'Bí tịch cổ ghi lại Ngự Kiếm Thuật; dùng để nhập môn.', tier: 2 },
  { id: 'bi-tich-thien-loi', name: 'Bí Tịch: Thiên Lôi Chỉ', glyph: '秘', rarity: 4, description: 'Bí tịch cổ ghi lại Thiên Lôi Chỉ; dùng để nhập môn.', tier: 2 },
  { id: 'bi-tich-dieu-hoa', name: 'Bí Tịch: Điều Hỏa Tán Quyết', glyph: '秘', rarity: 4, description: 'Bí tịch cổ ghi lại Điều Hỏa Tán Quyết; dùng để nhập môn.', tier: 2 },
  { id: 'bi-tich-ninh-dan', name: 'Bí Tịch: Ngưng Đan Kinh', glyph: '秘', rarity: 4, description: 'Bí tịch cổ ghi lại Ngưng Đan Kinh; dùng để nhập môn.', tier: 2 },
  { id: 'bi-tich-van-linh-lo', name: 'Bí Tịch: Vạn Linh Lô Quyết', glyph: '秘', rarity: 4, description: 'Bí tịch cổ ghi lại Vạn Linh Lô Quyết; dùng để nhập môn.', tier: 2 },
];
```

Wire vào phần upsert hiện có của seed: `MATERIALS` concat `MATERIALS_TIER2_CONGPHAP`, `CONG_PHAP` concat `CONG_PHAP_TIER2`. (Đọc phần seed hiện tại xem upsert theo pattern nào — PILLS/MATERIALS đã concat TIER2 từ Phase 1, làm y hệt.)

- [ ] **Step 3: Verify ngay trong file** — chạy `npx tsx -e "console.log('ok')"` không đủ: chạy seed thật lên dev DB (đúng semantic "catalog reset" đã biết):

Run: `cd backend && npm run db:seed && npm test`
Expected: seed không lỗi; 12 môn trong DB (3 cũ branch chienDao + 9 mới); full suite xanh.

- [ ] **Step 4: Commit** `git add backend/prisma/seed.ts && git commit -m "feat: seed cong phap tier 2 (9 mon) + bi tich + linh tai moi"`

---

### Task 11: Redeem material reward end-to-end (backend)

**Files:**
- Modify: `backend/src/domain/redeem/redeemCode.ts` (`RewardEntry`)
- Modify: `backend/src/domain/redeem/redeemCode.validate.ts`
- Modify: `backend/src/presentation/schemas/redeem.schemas.ts`
- Modify: `backend/src/application/RedeemCodeUseCase.ts` (+ inject `MaterialRepository`)
- Modify: `backend/src/infrastructure/repositories/PrismaRedeemCodeRepository.ts` (nếu mapping reward thủ công — đọc file xác nhận)
- Modify: `backend/src/app.ts` (truyền thêm repo vào use case)
- Test: `backend/src/domain/redeem/redeemCode.validate.test.ts`, `backend/src/application/redeem.congphap.test.ts` (thêm case material)

- [ ] **Step 1: Test fail**

```ts
// validate: reward 4 loại — đúng MỘT trong pillId/congPhapId/linhThach/materialId
it('chấp nhận reward materialId; chặn materialId + pillId cùng lúc', ...)
// use case: redeem code có material reward → MaterialRepository.increment(userId, id, qty) được gọi; result chứa { kind: 'material', id, name, glyph, quantity }
```

- [ ] **Step 2: FAIL** → **Step 3: Implement**

- `redeemCode.ts` RewardEntry: thêm `materialId?: string;` và sửa comment "Đúng một trong bốn khác undefined".
- `redeemCode.validate.ts`: `kinds` thêm `r.materialId !== undefined`; `key` thêm nhánh `material:${r.materialId}`; message cập nhật liệt kê 4 loại.
- `redeem.schemas.ts` rewardSchema: thêm `materialId: z.string().regex(/^[a-z0-9-]+$/).optional()`.
- `RedeemCodeUseCase`: constructor thêm `private readonly materials: MaterialRepository`; trong vòng lặp rewards thêm nhánh:

```ts
} else if (r.materialId) {
  await this.materials.increment(input.userId, r.materialId, r.quantity);
  const material = await this.materials.getById(r.materialId);
  results.push({ kind: 'material', id: r.materialId, name: material?.name ?? r.materialId, glyph: material?.glyph ?? '?', quantity: r.quantity });
}
```

- `redeemCode.ts` `RedeemRewardResult.kind` union: thêm `'material'` (kiểm tra type hiện tại).
- `app.ts`: truyền `materialRepository` vào `new RedeemCodeUseCase(...)`.

- [ ] **Step 4: Pass + suite + build** → **Step 5: Commit** `git commit -m "feat: redeem reward loai material (bi tich tam phat tang)`"

---

### Task 12: FE types/api/hook

**Files:**
- Modify: `frontend/src/lib/types.ts`
- Modify: `frontend/src/lib/api.ts`
- Modify: `frontend/src/hooks/use-cong-phap.ts` (nếu chưa có — xem tên hook hiện dùng trong `page.tsx` cho công pháp; mở rộng hook đó)

- [ ] **Step 1: Types** — `types.ts`:

```ts
export type CongPhapBranch = "tuLuyen" | "chienDao" | "danDao";
export type SystemEffectAttribute = "linhKhiRate" | "danDaoSuccess";
```

`PassiveEffectDTO.attribute`: mở rộng union `keyof AttributeSet | SystemEffectAttribute`. `CongPhapDTO` thêm: `tier: number; branch: CongPhapBranch | null; minRealmMajor: number; biTichMaterialId: string | null; biTichOwned: number;`. `CongPhapListResult` thêm `system: { linhKhiRatePct: number; danDaoSuccessPct: number }`. Thêm `LearnCongPhapResult { owned: string; linhThach: number; biTich: { id: string; quantity: number } }`. Redeem result kind union thêm `"material"`.

- [ ] **Step 2: api.ts**:

```ts
// POST /congphap/:id/learn — học môn bằng 1 Bí Tịch + 300 Linh Thạch.
export function learnCongPhap(congPhapId: string): Promise<LearnCongPhapResult> {
  return apiFetch<LearnCongPhapResult>(`/congphap/${congPhapId}/learn`, { method: "POST" });
}
```

- [ ] **Step 3: Hook** — expose `learn(id): Promise<void>` (gọi api → toast lỗi nếu throw → refetch) và giữ `system` tiếp ra component. Xem `use-alchemy-queue.ts` làm mẫu cho pattern clear/refetch.

- [ ] **Step 4: Verify** `cd frontend && pnpm exec tsc --noEmit && pnpm vitest run src && pnpm lint`

- [ ] **Step 5: Commit** `git add frontend/src && git commit -m "feat: fe types/api/hook cho hoc cong phap + system buffs"`

---

### Task 13: FE player UI — nhóm nhánh + nút học + breakdown

**Files:**
- Modify: `frontend/src/components/congphap-modal.tsx` (liệt kê/nhóm)
- Modify: `frontend/src/components/congphap-card.tsx` (nút học + lock reason, nếu card là nơi render từng môn)
- Modify: `frontend/src/components/attribute-panel.tsx` (hai dòng system buffs)
- Modify: `frontend/src/app/page.tsx` (handler learn + toast)

- [ ] **Step 1: Nhóm nhánh** — trong modal (đọc file để xác định component render catalog/owned): group `catalog` theo `branch` theo thứ tự cố định `tuLuyen (Tu Luyện) → chienDao (Chiến Đạo) → danDao (Đan Đạo) → null (Nhập Môn)`; render section header đơn giản (không cần CSS mới ngoài class có sẵn).

- [ ] **Step 2: Nút học** — môn có `biTichMaterialId !== null` và KHÔNG nằm trong `owned`: render nút `Học · 1 Bí Tịch + 300 LT` thay cho bảng level-up. Disabled khi: realm chưa đủ / `biTichOwned < 1` / linhThach < 300; lý do hiển thị ngay dưới nút bằng `<small className="alchemy-recipe-lock">` (class có sẵn): "Cần đạt cảnh giới <tên>" (realmName lấy từ realm list FE đã có) / "Thiếu Bí Tịch" / "Thiếu Linh Thạch". Môn đã sở hữu: UI level-up cũ không đổi.

- [ ] **Step 3: Handler + toast** — `page.tsx` thêm `handleLearnCongPhap` gọi hook; success toast `addToast("Công Pháp", \`Đã học ${name}\`, "success")`; lỗi toast danger (map message server trả về).

- [ ] **Step 4: Breakdown** — `attribute-panel.tsx` (đọc cấu trúc hiện có): thêm hai dòng sau khối attribute ẩn khi = 0: `Tốc độ tu luyện +{linhKhiRatePct}%` và `Hiệu suất luyện đan +{danDaoSuccessPct}%` (làm tròn 1 chữ số nếu lẻ).

- [ ] **Step 5: Verify** `cd frontend && pnpm exec tsc --noEmit && pnpm test && pnpm lint && pnpm build`

- [ ] **Step 6: Commit** `git add frontend/src && git commit -m "feat: nhom nhanh cong phap + nut hoc bi tich + breakdown buff he thong"`

---

### Task 14: FE admin — CongPhap editor + codes reward material

**Files:**
- Modify: `frontend/src/app/admin/congphap/page.tsx`
- Modify: `frontend/src/app/admin/codes/page.tsx` (reward form)
- Modify: `frontend/src/lib/admin-catalog-validation.ts` (mirror rules; file từ Phase 1)

- [ ] **Step 1: Mirror validation** — thêm rule: `tier ∈ 1..3`; `tier >= 2` ⇒ `branch` và `biTichMaterialId` bắt buộc; effect key đặc biệt ⇒ `flatPerLevel === 0 && pctPerLevel > 0`. Comment `// mirror backend domain/congphap/congphap.validate.ts`. Test unit FE cho 3 rule.

- [ ] **Step 2: Editor công pháp** — form thêm: select `tier` (1/2/3), select `branch` (Tu Luyện/Chiến Đạo/Đan Đạo/không), input `minRealmMajor` (int ≥ 0), select `biTichMaterialId` (lấy từ admin materials đã fetch — hiển thị `bi-tich-*` trước rồi đến material khác; cho phép "— không —"). Effect editor: attribute select thêm 2 option `linhKhiRate (Tốc độ tu luyện %)` và `danDaoSuccess (Hiệu suất luyện đan %)`; khi chọn key đặc biệt khóa input flat về 0.

- [ ] **Step 3: Codes reward** — reward editor thêm loại "Material": select id từ admin materials + quantity (giống pill select hiện có); kết quả redeem hiển thị icon material (glyph).

- [ ] **Step 4: Verify + Commit** — `pnpm exec tsc --noEmit && pnpm test && pnpm lint && pnpm build`; `git commit -m "feat: admin congphap tier/branch/bi tich + codes reward material"`

---

### Task 15: Acceptance + docs

**Files:**
- Modify: `README.md`, `CLAUDE.md`

- [ ] **Step 1: Chạy nghiệm thu §9 spec** — viết/đánh dấu từng mục (1–10) với evidence: lệnh chạy, output tóm tắt. Đặc biệt §9.2 (học môn + idempotent), §9.4 (preview +5 điểm ở cấp 5 môn đan), §9.5 (×1.2 rate kể cả offline), §9.8 (redeem material).
- [ ] **Step 2: Docs** — README bảng endpoint thêm `POST /congphap/:id/learn`; CLAUDE.md section Công Pháp 2.0 (bảng số: `LEARN_LINH_THACH_COST = 300`, `EffectAttribute`, gate `minRealmMajor` theo tier 1→0/2→3/3→5, mirror FE/BE validation).
- [ ] **Step 3: Full verify** — `cd backend && npm test && npm run build`; `cd frontend && pnpm test && pnpm lint && pnpm exec tsc --noEmit && pnpm build`.
- [ ] **Step 4: Commit** `git commit -m "docs: nghiem thu phase 2 + cap nhat readme/claude cong phap 2.0"`

---

## Self-review notes (đã kiểm trước khi lưu)

- Spec §4.1/§5/§6/§7/§8/§9 → task phủ: T1 (schema+migration+backfill), T2–T4 (mechanics domain), T5–T7 (learn flow §5.2), T8 (§5.3 tiêu thụ buff), T9 (§7 GET /cong-phap), T10 (§6 content), T11 (§7 redeem material), T12–T14 (§7 FE/admin), T15 (§9).
- Kiểu trả về `learnWithCosts` nhất quán T5↔T6; tên `buildSystemBuffs`/`sumSystemBuffs` nhất quán T2↔T8↔T9; `CONGPHAP_NOT_LEARNABLE/CONGPHAP_REALM_GATE/CONGPHAP_MISSING_BITICH/CONGPHAP_ALREADY_OWNED` nhất quán T6↔T7↔T13.
- T8 `AttemptBreakthroughUseCase` constructor đổi → cập nhật cả test file hiện có của nó nếu có (fixture constructor args).
