# Bí Cảnh 2.0 (Phase 3) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Tầng 1–3 cho 8 nhánh bí cảnh (gate cảnh giới hard + chiến lực đề xuất soft), loadout 2 đan combat vào mô phỏng, boss drop Bí Tịch + Đan Hỏa Tủy.

**Architecture:** Toàn additive: 3 cột mới trên `ExpeditionBranch` + bảng `ExpeditionBossDropWeight` + 2 cột trên `Pill` (effectKind mới `combatBuff`). Domain: `expeditionStartGate` + `validateLoadout` thuần; `simulateBattle` nhận optional `pillBuffs`; `rollExpeditionRewards` roll bảng boss riêng. Trừ đan loadout trong transaction `ExpeditionRepository.start`.

**Tech Stack:** Express + Prisma + PostgreSQL (backend), Next.js + Biome + Vitest (frontend). Spec: `docs/superpowers/specs/2026-08-07-bi-canh-2-design.md`.

**Lệnh chuẩn:** backend `cd backend && npm test` (chạy tất), `npx vitest run <file>` (chạy nhanh 1 file); frontend `cd frontend && pnpm vitest run src`, `pnpm lint`, `pnpm exec tsc --noEmit`, `pnpm build`. Dev DB: `localhost:5432`, user `game`, DB `tu_tien_chi_lo` (docker `backend-db-1`). Migration diff cần `--shadow-database-url postgresql://game:game@localhost:5432/shadow_congphap2`.

---

### Task 1: Schema + migration + domain types

**Files:**
- Modify: `backend/prisma/schema.prisma` (model ExpeditionBranch, Pill; thêm model ExpeditionBossDropWeight)
- Create: `backend/prisma/migrations/<timestamp>_bi_canh_2/migration.sql`
- Modify: `backend/src/domain/expedition/expedition.ts` (ExpeditionBranchConfig +3 field, bossDropWeights; LoadoutEntry/CombatBuffTrigger; ExpeditionCombatSnapshot.loadout?)
- Test: `backend/tests/integration/` (full suite compile check)

- [ ] **Step 1: Sửa schema.prisma**

Trong `model ExpeditionBranch`, sau `alchemyMaterial`:
```prisma
  tier              Int                             @default(1)
  minRealmMajor     Int                             @default(0)
  recommendedPower  Float                           @default(0)
  bossDropWeights   ExpeditionBossDropWeight[]
```

Trong `model Pill`, sau `bonusPct`:
```prisma
  combatAttribute String?
  combatTrigger   String?
```

Cuối file thêm:
```prisma
model ExpeditionBossDropWeight {
  id         String           @id @default(uuid())
  branchId   String
  materialId String
  weight     Float
  branch     ExpeditionBranch @relation(fields: [branchId], references: [id], onDelete: Cascade)
  material   Material         @relation(fields: [materialId], references: [id])

  @@unique([branchId, materialId])
}
```
Thêm vào `model Material`: `bossDropWeights ExpeditionBossDropWeight[]` (relation đối ứng).

- [ ] **Step 2: Tạo migration qua diff**

```bash
cd backend && npx prisma migrate diff \
  --from-schema-datasource prisma/schema.prisma \
  --to-schema-datamodel prisma/schema.prisma \
  --shadow-database-url postgresql://game:game@localhost:5432/shadow_congphap2 \
  --script > prisma/migrations/20260807190000_bi_canh_2/migration.sql
```
(mkdir `prisma/migrations/20260807190000_bi_canh_2` trước). Thêm cuối file backfill:
```sql
UPDATE "ExpeditionBranch" SET tier = 2, "minRealmMajor" = 3, "recommendedPower" = 600
  WHERE id IN ('thanh-lam', 'u-minh', 'van-hai');
UPDATE "ExpeditionBranch" SET tier = 3, "minRealmMajor" = 5, "recommendedPower" = 1200
  WHERE id IN ('long-mach', 'tinh-thien');
```
Áp dụng: `docker exec -i backend-db-1 psql -U game -d tu_tien_chi_lo < prisma/migrations/20260807190000_bi_canh_2/migration.sql` rồi `npx prisma migrate resolve --applied 20260807190000_bi_canh_2` + `npx prisma generate`. Kiểm `npx prisma migrate status` sạch.

- [ ] **Step 3: Domain types (`backend/src/domain/expedition/expedition.ts`)**

Thêm sau import:
```ts
import { AttributeSet } from '../attributes/attributes';

export type CombatBuffTrigger = 'start' | 'lowHp30';

export interface LoadoutEntry {
  pillId: string;
  combatAttribute: keyof AttributeSet;
  combatTrigger: CombatBuffTrigger;
  pct: number; // từ Pill.bonusPct
}
```

`ExpeditionBranchConfig` thêm:
```ts
  // Phase 3 — tầng 1..3 (Phàm/Linh/Thiên); gate cảnh giới hard, chiến lực soft.
  tier: number;
  minRealmMajor: number;
  recommendedPower: number;
  bossDropWeights: ExpeditionUpgradeMaterialWeight[]; // chỉ boss encounter roll
```

`ExpeditionCombatSnapshot` thêm:
```ts
  loadout?: LoadoutEntry[];
```

- [ ] **Step 4: Sửa fixture compile (3 file dựng ExpeditionBranchConfig)**

`backend/src/domain/expedition/expedition.calc.test.ts` — trong `const branch: ExpeditionBranchConfig = {`, thêm sau `upgradeMaterialWeights: [...],`:
```ts
  tier: 1, minRealmMajor: 0, recommendedPower: 0, bossDropWeights: [],
```

`backend/src/application/expedition.usecases.test.ts` (~dòng 10) — sau `upgradeMaterialWeights: [...]` thêm cùng dòng trên.

`backend/src/application/admin.expedition-catalog.test.ts` + `backend/src/infrastructure/repositories/expedition.repo.integration.test.ts` + `backend/src/infrastructure/repositories/admin.expedition-catalog.repo.integration.test.ts` — chỗ nào dựng `branch`/`ExpeditionBranchConfig` literal, thêm `tier: 1, minRealmMajor: 0, recommendedPower: 0, bossDropWeights: [],` (grep `upgradeMaterialWeights` để tìm hết).

- [ ] **Step 5: Verify compile + run expedition suites**

Run: `cd backend && npx vitest run src/domain/expedition 2>&1 | tail -3`
Expected: PASS (hành vi cũ không đổi — chưa ai đọc field mới). TypeScript error ở chỗ nào thì sửa fixture đó.

- [ ] **Step 6: Commit**

```bash
git add -A && git commit -m "feat: Phase 3 schema (tang expedition + bossDropWeights + pill combat fields)"
```

---

### Task 2: Repo mapping (config + admin catalog)

**Files:**
- Modify: `backend/src/infrastructure/repositories/PrismaExpeditionConfigRepository.ts`
- Modify: `backend/src/infrastructure/repositories/PrismaExpeditionCatalogAdminRepository.ts`
- Test: `backend/src/infrastructure/repositories/admin.expedition-catalog.repo.integration.test.ts`

- [ ] **Step 1: Thêm test round-trip (failing)**

Trong `admin.expedition-catalog.repo.integration.test.ts`, thêm case: replace một bundle với `tier: 2, minRealmMajor: 3, recommendedPower: 600, bossDropWeights: [{ materialId: 'dan-hoa-tuy', weight: 0.3 }]` (tạo material `dan-hoa-tuy` trước trong test, xem pattern test hiện có tạo material), rồi `list()` assert đọc lại đúng 4 giá trị.

- [ ] **Step 2: Watch fail** — `npx vitest run src/infrastructure/repositories/admin.expedition-catalog.repo.integration.test.ts | tail -5` → FAIL (field mất khi toBundle/replace không map).

- [ ] **Step 3: Implement mapping**

`PrismaExpeditionConfigRepository.ts`: `toBundle` signature row thêm `tier: number; minRealmMajor: number; recommendedPower: number;` và `bossDropWeights: { materialId: string; weight: number }[];` — include thêm `bossDropWeights: { orderBy: { materialId: 'asc' } }` trong cả `listBranches`/`getBranch`. Branch build thêm:
```ts
  tier: row.tier,
  minRealmMajor: row.minRealmMajor,
  recommendedPower: row.recommendedPower,
  bossDropWeights: row.bossDropWeights.map((weight) => ({ materialId: weight.materialId, weight: weight.weight })),
```

`PrismaExpeditionCatalogAdminRepository.replace()`: upsert `expeditionBranch` create+update thêm `tier: row.branch.tier, minRealmMajor: row.branch.minRealmMajor, recommendedPower: row.branch.recommendedPower`. Sau blok `expeditionUpgradeMaterialWeight` thêm:
```ts
        for (const weight of row.branch.bossDropWeights) {
          if (!await tx.material.findUnique({ where: { id: weight.materialId }, select: { id: true } })) {
            throw new DomainError('MATERIAL_NOT_FOUND', `boss drop material not found: ${weight.materialId}`);
          }
        }
        await tx.expeditionBossDropWeight.deleteMany({ where: { branchId: row.branch.id } });
        await tx.expeditionBossDropWeight.createMany({ data: row.branch.bossDropWeights.map((weight) => ({ branchId: row.branch.id, materialId: weight.materialId, weight: weight.weight })) });
```

- [ ] **Step 4: Watch pass + chạy cả 2 repo test**: `npx vitest run src/infrastructure/repositories/admin.expedition-catalog.repo.integration.test.ts src/infrastructure/repositories/expedition.repo.integration.test.ts | grep "Tests "` → PASS.

- [ ] **Step 5: Commit** — `git add -A && git commit -m "feat: map tier/gate/bossDropWeights o expedition repos"`

---

### Task 3: `expeditionStartGate` (domain)

**Files:**
- Modify: `backend/src/domain/expedition/expedition.calc.ts` + `expedition.calc.test.ts`

- [ ] **Step 1: Test failing** — thêm vào `expedition.calc.test.ts`:
```ts
describe('expeditionStartGate', () => {
  it('chặn khi realm thấp hơn gate, message mang tên cảnh giới', () => {
    expect(() => expeditionStartGate({ ...branch, tier: 2, minRealmMajor: 3 }, 2, 'Kết Đan'))
      .toThrowError(/EXPEDITION_REALM_GATE|Tầng 2 yêu cầu cảnh giới Kết Đan/);
  });
  it('qua khi đủ cảnh', () => {
    expect(() => expeditionStartGate({ ...branch, tier: 2, minRealmMajor: 3 }, 3, 'Kết Đan')).not.toThrow();
  });
  it('tầng 1 không gate (minRealmMajor 0)', () => {
    expect(() => expeditionStartGate(branch, 0, 'Phàm Nhân')).not.toThrow();
  });
});
```
Import: `expeditionStartGate` từ `./expedition.calc`.

- [ ] **Step 2: Watch fail** → `expeditionStartGate is not a function`.

- [ ] **Step 3: Implement** (cuối `expedition.calc.ts`):
```ts
// Phase 3 — hard gate cảnh giới theo tầng (chiến lực recommendedPower chỉ là
// cảnh báo phía FE, server không chặn). realmName đã resolve ở application
// layer để domain không phụ thuộc realm catalog.
export function expeditionStartGate(
  branch: Pick<ExpeditionBranchConfig, 'tier' | 'minRealmMajor'>,
  realmMajor: number,
  realmName: string,
): void {
  if (realmMajor < branch.minRealmMajor) {
    throw new DomainError('EXPEDITION_REALM_GATE', `Tầng ${branch.tier} yêu cầu cảnh giới ${realmName}`);
  }
}
```

- [ ] **Step 4: Watch pass.**

- [ ] **Step 5: Commit** — `"feat: expeditionStartGate (hard realm gate theo tang)"`.

---

### Task 4: `validateLoadout` (domain)

**Files:**
- Create: `backend/src/domain/expedition/expedition.loadout.ts` + `expedition.loadout.test.ts`

- [ ] **Step 1: Test failing** — `backend/src/domain/expedition/expedition.loadout.test.ts`:
```ts
import { describe, expect, it } from 'vitest';
import { validateLoadout } from './expedition.loadout';
import { PillRecord } from '../pills/pill';

function combatPill(id: string, overrides: Partial<PillRecord> = {}): PillRecord {
  return {
    id, name: id, glyph: '丹', rarity: 3, tier: 2, effectKind: 'combatBuff',
    amount: null, multiplier: null, durationSec: null, bonusPct: 25,
    combatAttribute: 'congVatLy', combatTrigger: 'start',
    desc: 'd', active: true, starterQuantity: 0, ...overrides,
  };
}

describe('validateLoadout', () => {
  it('trả entries chuẩn cho 2 đan combatBuff hợp lệ', () => {
    const entries = validateLoadout([combatPill('a'), combatPill('b', { combatTrigger: 'lowHp30', bonusPct: 35 })]);
    expect(entries).toEqual([
      { pillId: 'a', combatAttribute: 'congVatLy', combatTrigger: 'start', pct: 25 },
      { pillId: 'b', combatAttribute: 'congVatLy', combatTrigger: 'lowHp30', pct: 35 },
    ]);
  });
  it('rỗng khi không chọn đan', () => {
    expect(validateLoadout([])).toEqual([]);
  });
  it.each([
    ['quá 2 slot', [combatPill('a'), combatPill('b'), combatPill('c')]],
    ['trùng id', [combatPill('a'), combatPill('a')]],
    ['đan không active', [combatPill('a', { active: false })]],
    ['sai kind (linhKhi)', [combatPill('a', { effectKind: 'linhKhi', amount: 50 })]],
    ['bonusPct không dương', [combatPill('a', { bonusPct: 0 })]],
    ['combatAttribute lạ', [combatPill('a', { combatAttribute: 'xyz' as never })]],
    ['combatTrigger lạ', [combatPill('a', { combatTrigger: 'mid' as never })]],
  ])('LOADOUT_INVALID khi %s', (_label, pills) => {
    expect(() => validateLoadout(pills)).toThrowError(/LOADOUT_INVALID/);
  });
});
```

- [ ] **Step 2: Watch fail.**

- [ ] **Step 3: Implement** — `backend/src/domain/expedition/expedition.loadout.ts`:
```ts
import { ATTRIBUTE_KEYS } from '../attributes/attributes';
import { DomainError } from '../errors';
import { PillRecord } from '../pills/pill';
import { LoadoutEntry } from './expedition';

const TRIGGERS = new Set(['start', 'lowHp30']);

// tối đa 2 slot; chỉ đan combatBuff còn active; field đủ và đúng tập cho phép.
export function validateLoadout(pills: readonly PillRecord[]): LoadoutEntry[] {
  if (pills.length > 2) throw new DomainError('LOADOUT_INVALID', 'Loadout tối đa 2 đan');
  const seen = new Set<string>();
  return pills.map((pill) => {
    if (seen.has(pill.id)) throw new DomainError('LOADOUT_INVALID', 'Không thể mang hai viên cùng loại');
    seen.add(pill.id);
    if (!pill.active || pill.effectKind !== 'combatBuff') {
      throw new DomainError('LOADOUT_INVALID', `Đan không dùng được cho bí cảnh: ${pill.id}`);
    }
    if (!(pill.bonusPct !== null && pill.bonusPct > 0)) throw new DomainError('LOADOUT_INVALID', `Đan ${pill.id} thiếu bonusPct`);
    if (!pill.combatAttribute || !ATTRIBUTE_KEYS.includes(pill.combatAttribute)) {
      throw new DomainError('LOADOUT_INVALID', `Đan ${pill.id} combatAttribute không hợp lệ`);
    }
    if (!pill.combatTrigger || !TRIGGERS.has(pill.combatTrigger)) {
      throw new DomainError('LOADOUT_INVALID', `Đan ${pill.id} combatTrigger không hợp lệ`);
    }
    return { pillId: pill.id, combatAttribute: pill.combatAttribute, combatTrigger: pill.combatTrigger, pct: pill.bonusPct };
  });
}
```

- [ ] **Step 4: Watch pass.** (Lưu ý: test trên giả định Task 6 đã thêm `combatBuff` vào PillEffectKind + combatAttribute/combatTrigger vào PillRecord — **chạy Task 6 trước Task này trong compile**, hoặc tạm `as never`. **Quyết định: đổi thứ tự — Task 6 (pill kind) chạy TRƯỚC Task 4.** Plan này liệt kê theo thứ tự đã sắp: thực thi theo thứ tự 1→2→3→**6**→4→5→7→...)

- [ ] **Step 5: Commit** — `"feat: validateLoadout (2 slot dan combatBuff)"`.

---

### Task 5: `simulateBattle` nhận `pillBuffs` + `simulateExpedition` truyền qua

**Files:**
- Modify: `backend/src/domain/combat/combat.calc.ts` (+ `combat.calc.test.ts`)
- Modify: `backend/src/domain/expedition/expedition.calc.ts` (+ `expedition.calc.test.ts`)

- [ ] **Step 1: Test failing (combat)** — thêm describe trong `combat.calc.test.ts` (file hiện có; tham chiếu style test battle sẵn có):
```ts
describe('simulateBattle với pillBuffs', () => {
  const weakPlayer: CombatantSnapshot = {
    id: 'player', attributes: { khiHuyet: 500, chanNguyen: 0, congVatLy: 50, congPhep: 0, phongThu: 0, tocDo: 10 },
    battlePower: 100, maxChanNguyen: 0, skills: [],
  };
  const testBuff = (attr: string): LoadoutEntry => ({ pillId: 'p1', combatAttribute: attr as never, combatTrigger: 'start', pct: 100 });

  it('trigger start: buff congVatLy áp ngay từ lượt 1', () => {
    const noBuff = simulateBattle({ player: weakPlayer, enemy: enemy, random: new ConstantRandom(0.5), maxTurns: 1 });
    const buffed = simulateBattle({ player: weakPlayer, enemy: enemy, random: new ConstantRandom(0.5), maxTurns: 1, pillBuffs: [testBuff('congVatLy')] });
    const dmgNoBuff = noBuff.turns.find((t) => t.actor === 'player')?.damage ?? 0;
    const dmgBuffed = buffed.turns.find((t) => t.actor === 'player')?.damage ?? 0;
    // 50 → 100 công: damage ~ (1± 0.1 variance với random 0.5 → hệ số 1.0)
    expect(Math.round(dmgBuffed / dmgNoBuff)).toBe(2);
  });

  it('trigger lowHp30: chỉ kích khi HP player ≤ 30% max', () => {
    // player rất yếu: enemy đánh trước mạnh → sau lượt enemy HP < 30% → buff khiHuyet kích, HP tăng thêm delta
    const fragile: CombatantSnapshot = { ...weakPlayer, attributes: { ...weakPlayer.attributes, khiHuyet: 100, tocDo: 1 } };
    const strongEnemy: CombatantSnapshot = { id: 'enemy', attributes: { khiHuyet: 100_000, chanNguyen: 0, congVatLy: 45, congPhep: 0, phongThu: 0, tocDo: 50 }, battlePower: 999, maxChanNguyen: 0, skills: [] };
    const result = simulateBattle({
      player: fragile, enemy: strongEnemy, random: new ConstantRandom(0.5), maxTurns: 5,
      pillBuffs: [{ pillId: 'hp-pill', combatAttribute: 'khiHuyet', combatTrigger: 'lowHp30', pct: 100 }],
    });
    // HP 100 → enemy đánh → ~55 → buff kích: maxHp 200, hp = 55 + 100 = 155
    const hpTurn = result.turns.find((t) => t.actor === 'enemy');
    expect(result.turns.some((t) => t.remainingHp > 100 && t.target === 'enemy')).toBe(false); // sanity: buff không buff enemy
    expect(result.playerRemainingHp).toBeGreaterThan(0); // buff cứu một mạng (enemy mỗi hit ~45)
  });

  it('lowHp30 không kích khi HP không chạm ngưỡng', () => {
    const tank: CombatantSnapshot = { ...weakPlayer, attributes: { ...weakPlayer.attributes, khiHuyet: 100_000 } };
    const result = simulateBattle({
      player: tank, enemy: strongEnemyMin, random: new ConstantRandom(0.5), maxTurns: 3,
      pillBuffs: [{ pillId: 'hp-pill', combatAttribute: 'khiHuyet', combatTrigger: 'lowHp30', pct: 100 }],
    });
    // không turn nào có damage bất thường từ buff — tương đương không buff:
    const plain = simulateBattle({ player: tank, enemy: strongEnemyMin, random: new ConstantRandom(0.5), maxTurns: 3 });
    expect(result.turns.map((t) => t.damage)).toEqual(plain.turns.map((t) => t.damage));
  });

  it('không mutate snapshot đầu vào (battle sau buff start lại từ đầu)', () => {
    const snapshot = { ...weakPlayer, attributes: { ...weakPlayer.attributes } };
    simulateBattle({ player: snapshot, enemy, random: new ConstantRandom(0.5), maxTurns: 2, pillBuffs: [testBuff('congVatLy')] });
    expect(snapshot.attributes.congVatLy).toBe(50);
  });
});
```
(Viết lại fixture enemy/ConstantRandom theo style test hiện có trong file; các số cloud trên là kỳ vọng với variance 0.9+0.5*0.2=1.0 tại random 0.5.)

- [ ] **Step 2: Watch fail** → `pillBuffs` chưa được đọc.

- [ ] **Step 3: Implement `combat.calc.ts`**

Thêm import + type:
```ts
import { LoadoutEntry } from '../expedition/expedition';
```
(Lưu ý circular: `expedition.calc.ts` import `combat.calc.ts`; `combat.calc.ts` import type từ `expedition/expedition.ts` (types-only, expedition.ts KHÔNG import combat.calc) → không vòng lặp runtime. Dùng `import type`.)

`CombatState` thêm `maxHp: number`. `createState`: `maxHp: snapshot.attributes.khiHuyet` (kèm `hp`).

`simulateBattle` input thêm `pillBuffs?: LoadoutEntry[]`; sau `createState`:
```ts
  // Đan combat (Phase 3): buff 'start' áp ngay; 'lowHp30' kích một lần khi HP
  // player <= 30% max. Buff tính trên bản copy attribute để không rò sang
  // encounter kế tiếp (simulateExpedition tái dùng snapshot gốc).
  applyLoadoutBuffs(states.player, (input.pillBuffs ?? []).filter((buff) => buff.combatTrigger === 'start'));
```
Trong vòng `for (const actorId of order)`, sau khi `turns.push(...)` và khi target là player và player còn sống:
```ts
      if (targetId === 'player' && target.hp > 0 && target.hp <= 0.3 * target.maxHp) {
        const pending = (input.pillBuffs ?? []).filter(
          (buff) => buff.combatTrigger === 'lowHp30' && !lowHpFired.has(buff.pillId),
        );
        if (pending.length > 0) {
          applyLoadoutBuffs(target, pending);
          for (const buff of pending) lowHpFired.add(buff.pillId);
        }
      }
```
khai `const lowHpFired = new Set<string>();` cạnh `turns`.

Helper:
```ts
function applyLoadoutBuffs(state: CombatState, buffs: readonly LoadoutEntry[]): void {
  if (buffs.length === 0) return;
  const attrs = { ...state.snapshot.attributes };
  const pctByAttr = new Map<string, number>();
  for (const buff of buffs) pctByAttr.set(buff.combatAttribute, (pctByAttr.get(buff.combatAttribute) ?? 0) + buff.pct);
  const oldMax = state.maxHp;
  for (const [attr, pct] of pctByAttr) {
    if (typeof attrs[attr as keyof typeof attrs] === 'number') {
      (attrs as Record<string, number>)[attr] *= 1 + pct / 100;
    }
  }
  state.snapshot = { ...state.snapshot, attributes: attrs };
  if (pctByAttr.has('khiHuyet')) {
    state.maxHp = attrs.khiHuyet;
    state.hp = Math.min(state.maxHp, state.hp + (state.maxHp - oldMax));
  }
}
```

- [ ] **Step 4: `simulateExpedition` truyền buffs** — `expedition.calc.ts` signature thêm `pillBuffs?: LoadoutEntry[]` và trong vòng encounter:
```ts
    const battle = simulateBattle({
      player: input.player,
      enemy: enemyForPower(power, input.player.attributes.tocDo),
      random: input.random,
      maxTurns: input.maxTurns,
      pillBuffs: input.pillBuffs,
    });
```
Thêm test trong `expedition.calc.test.ts`: simulateExpedition với `pillBuffs: [{start, congVatLy, 50}]` → `encounters[1]` (battle thứ 2) cũng nhận buff (damage encounter 2 > cùng fixture không buff) — chứng minh buff reset-đúng qua các encounter:

```ts
it('simulateExpedition áp pillBuffs cho mọi encounter (reset mỗi battle)', () => {
  const buff = { pillId: 'p', combatAttribute: 'congVatLy' as const, combatTrigger: 'start' as const, pct: 100 };
  const plain = simulateExpedition({ ...baseInput });
  const buffed = simulateExpedition({ ...baseInput, pillBuffs: [buff] });
  const dmgOf = (sim: typeof plain, idx: number) =>
    sim.encounters[idx]?.result.turns.filter((t) => t.actor === 'player').reduce((s, t) => s + t.damage, 0) ?? 0;
  expect(dmgOf(buffed, 0)).toBeGreaterThan(dmgOf(plain, 0));
  expect(dmgOf(buffed, 1)).toBeGreaterThan(dmgOf(plain, 1));
});
```

- [ ] **Step 5: Watch pass cả 2 file test.**

- [ ] **Step 6: Commit** — `"feat: simulateBattle pillBuffs (start + lowHp30), simulateExpedition truyen qua"`.

---

### Task 6: Pill `combatBuff` end-to-end domain/presentation validate

**Files:**
- Modify: `backend/src/domain/pills/pill.ts` (PillEffectKind + PillRecord +2 field), `pill.validate.ts`, `pill.calc.ts`
- Modify: `backend/src/presentation/schemas/admin.schemas.ts`
- Modify: `backend/src/infrastructure/repositories/PrismaPillRepository.ts` (mapping 2 cột)
- Test: `backend/src/domain/pills/pill.validate.test.ts`(file hiện có — grep tên chính xác khi chạy), `backend/tests/unit/.../pills-schema.test.ts`, `PrismaPillRepository.test.ts`

- [ ] **Step 1: Test failing** — validate:
```ts
it('combatBuff hợp lệ khi bonusPct>0 + combatAttribute + combatTrigger đúng', () => {
  expect(() => validatePillDefinition(basePill({
    effectKind: 'combatBuff', bonusPct: 25,
    combatAttribute: 'congVatLy', combatTrigger: 'start',
    amount: null, multiplier: null, durationSec: null,
  }))).not.toThrow();
});
it.each([
  ['thiếu bonusPct', { bonusPct: null }],
  ['bonusPct âm', { bonusPct: -5 }],
  ['thiếu combatAttribute', { combatAttribute: null }],
  ['combatAttribute lạ', { combatAttribute: 'mana' }],
  ['combatTrigger lạ', { combatTrigger: 'mid' }],
  ['amount lẻ', { amount: 10 }],
])('combatBuff reject: %s', (_label, overrides) => {
  expect(() => validatePillDefinition(basePill({ effectKind: 'combatBuff', bonusPct: 25, combatAttribute: 'congVatLy', combatTrigger: 'start', ...overrides } as never))).toThrowError(/INVALID_PILL_CONFIG/);
});
```

`pill.calc`/consume: test `applyPillEffect({ ...combatBuff pill })` → throws `PILL_NOT_CONSUMABLE` (đan combat chỉ dùng qua loadout):
```ts
it('applyPillEffect reject đan combatBuff (chỉ dùng cho loadout bí cảnh)', () => {
  expect(() => applyPillEffect({ currentLinhKhi: 0, character: baseChar, pill: combatPill, now })).toThrowError(/PILL_NOT_CONSUMABLE/);
});
```

Schema: thêm vào `pills-schema.test.ts` case parse body `effectKind: 'combatBuff'` với `combatAttribute`/`combatTrigger` → success.

- [ ] **Step 2: Watch fail.**

- [ ] **Step 3: Implement**

`pill.ts`:
```ts
export type PillEffectKind =
  | 'linhKhi'
  | 'cultivationBuff'
  | 'breakthroughBoost'
  | 'clearPunishment'
  // Phase 3 — đan combat: chỉ dùng qua loadout bí cảnh, không consume trực tiếp.
  | 'combatBuff';
```
`PillRecord` thêm:
```ts
  // Phase 3 combatBuff: attribute bị buff trong simulateBattle + thời điểm kích.
  combatAttribute: keyof AttributeSet | null;
  combatTrigger: 'start' | 'lowHp30' | null;
```
(import type AttributeSet.)

`pill.validate.ts`: `KIND_FIELDS` thêm `combatBuff: ['bonusPct']` (cascade orphan tự có). Trước orphan check thêm:
```ts
  if (pill.effectKind === 'combatBuff') {
    if (!(pill.bonusPct !== null && pill.bonusPct > 0)) fail('combatBuff pills require bonusPct > 0');
    if (!pill.combatAttribute || !ATTRIBUTE_KEYS.includes(pill.combatAttribute)) fail('combatBuff pills require a valid combatAttribute');
    if (pill.combatTrigger !== 'start' && pill.combatTrigger !== 'lowHp30') fail('combatBuff pills require combatTrigger start|lowHp30');
  } else {
    if (pill.combatAttribute !== null) fail('combatAttribute must be null unless effectKind is "combatBuff"');
    if (pill.combatTrigger !== null) fail('combatTrigger must be null unless effectKind is "combatBuff"');
  }
```
(import ATTRIBUTE_KEYS.)

`pill.calc.ts` switch thêm:
```ts
    case 'combatBuff':
      // Đan combat không tiêu thụ trực tiếp — chỉ mang vào loadout bí cảnh.
      throw new DomainError('PILL_NOT_CONSUMABLE', 'Đan combat chỉ dùng cho loadout bí cảnh');
```
(import DomainError.)

`admin.schemas.ts` pillBodySchema:
```ts
  effectKind: z.enum(['linhKhi', 'cultivationBuff', 'breakthroughBoost', 'clearPunishment', 'combatBuff']),
  combatAttribute: z.enum(['khiHuyet', 'chanNguyen', 'congVatLy', 'congPhep', 'phongThu', 'tocDo']).nullable().default(null),
  combatTrigger: z.enum(['start', 'lowHp30']).nullable().default(null),
```

`PrismaPillRepository.ts`: mọi nơi build `PillRecord` từ row → thêm `combatAttribute: row.combatAttribute as PillRecord['combatAttribute'], combatTrigger: row.combatTrigger as PillRecord['combatTrigger']`, và chỗ create/update ghi 2 cột.

**Fixtures phải sửa thêm 2 field** `combatAttribute: null, combatTrigger: null`: grep `effectKind:` trong `backend/src`, `backend/tests` (mọi chỗ dựng PillRecord literal: pill calc/validate/use-case/admin tests, in-memory fakes, route tests).

- [ ] **Step 4: Watch pass** `npx vitest run src/domain/pills tests 2>&1 | grep "Tests "`.

- [ ] **Step 5: Wire error code** — `errorHandler.ts` thêm `PILL_NOT_CONSUMABLE: 400` (và có sẵn ở Task 8: `EXPEDITION_REALM_GATE: 409, LOADOUT_INVALID: 400`).

- [ ] **Step 6: Commit** — `"feat: pill effectKind combatBuff (validate + consume guard + admin schema)"`.

---

### Task 7: Boss drop trong `rollExpeditionRewards`

**Files:** `backend/src/domain/expedition/expedition.calc.ts` + test.

- [ ] **Step 1: Test failing**:
```ts
it('boss roll thêm từ bossDropWeights (quantity 1) khi trúng rate', () => {
  const bossBranch: ExpeditionBranchConfig = { ...branch, bossDropWeights: [{ materialId: 'bi-tich-vong-coc', weight: 1 }] };
  const reward = rollExpeditionRewards({ branch: bossBranch, difficulty: { ...easy, bossDropRate: 1, normalDropRate: 0 }, ticketCostUnits: 1, wins: 3, random: new ConstantRandom(0.01) });
  expect(reward.materials.find((m) => m.materialId === 'bi-tich-vong-coc')?.quantity).toBe(1);
});
it('bảng boss rỗng → hành vi cũ y nguyên', () => {
  const reward = rollExpeditionRewards({ branch, difficulty: { ...easy, bossDropRate: 1, normalDropRate: 0 }, ticketCostUnits: 1, wins: 3, random: new ConstantRandom(0.01) });
  expect(reward.materials.every((m) => !m.materialId.startsWith('bi-tich'))).toBe(true);
});
```

- [ ] **Step 2: Implement** — trong `rollExpeditionRewards`, inner loop sau hai `if` roll hiện có:
```ts
    if (kind === 'boss' && input.branch.bossDropWeights.length > 0 && input.random.next() < chance) {
      const selected = weightedMaterial(input.branch.bossDropWeights, input.random.next());
      if (selected) addMaterial(materials, selected, 1);
    }
```

- [ ] **Step 3: Watch pass** → commit `"feat: boss roll rieng (bi tich + dan hoa tuy)"`.

---

### Task 8: StartExpeditionUseCase — gate + loadout + trừ tồn kho

**Files:**
- Modify: `backend/src/application/StartExpeditionUseCase.ts` (+ `expedition.usecases.test.ts`)
- Modify: `backend/src/domain/ports/ExpeditionRepository.ts` (`start` input + `loadoutConsumptions`)
- Modify: `backend/src/infrastructure/repositories/PrismaExpeditionRepository.ts`
- Modify: `backend/src/domain/ports/PillRepository.ts` (nếu chưa có `listByIds` — kiểm `GetInventoryUseCase` dùng gì; nếu có `getById`/`listAll` thì dùng nó)
- Test: `backend/src/infrastructure/repositories/expedition.repo.integration.test.ts` (+2 case decrement)

- [ ] **Step 1: Test use case (failing)** — trong `expedition.usecases.test.ts` thêm:
```ts
it('chặn start khi thiếu cảnh giới (EXPEDITION_REALM_GATE)', async () => {
  const f = makeFixture({ realmMajor: 0 });
  await expect(new StartExpeditionUseCase(f.config as never, f.repo as never, f.cultivation as never, f.owned as never, f.pills as never, f.random as never)
    .execute(userId, { branchId: 'thanh-lam', difficulty: 'easy', durationSec: 1_800 }))
    .rejects.toThrowError(/EXPEDITION_REALM_GATE/);
});

it('loadout hợp lệ: snapshot chứa loadout, repo nhận loadoutConsumptions', async () => {
  const f = makeFixture({ realmMajor: 3, inventory: { 'cuong-the-dan': 2 } });
  const repo = spyRepo();
  await new StartExpeditionUseCase(f.config as never, repo as never, f.cultivation as never, f.owned as never, f.pills as never, f.random as never)
    .execute(userId, { branchId: 'thanh-lam', difficulty: 'easy', durationSec: 1_800, loadoutPillIds: ['cuong-the-dan'] });
  expect(repo.lastStart.combatSnapshot.loadout).toEqual([{ pillId: 'cuong-the-dan', combatAttribute: 'congVatLy', combatTrigger: 'start', pct: 25 }]);
  expect(repo.lastStart.loadoutConsumptions).toEqual([{ pillId: 'cuong-the-dan', quantity: 1 }]);
});

it('LOADOUT_INVALID khi đan không phải combatBuff', async () => { ... rejects });
```

(Fixture `makeFixture` dựng theo style test hiện có: config repo fake trả branch tier2, cultivation fake trả realmMajor, pills fake trả PillRecord.)

- [ ] **Step 2: Test repo (integration, failing)** — `expedition.repo.integration.test.ts`:
```ts
it('start với loadoutConsumptions trừ tồn kho nguyên tử; thiếu → INSUFFICIENT_INVENTORY', async () => {
  // tạo pill combatBuff + InventoryItem quantity 1; start với loadoutConsumptions [{pillId, quantity: 1}]
  // → expedition tạo, inventory còn 0. start tiếp (đủ vé, kỳ này quantity 1 nữa) → INSUFFICIENT_INVENTORY, không tạo expedition mới.
});
```

- [ ] **Step 3: Implement**

`ExpeditionRepository.start` input thêm:
```ts
    loadoutConsumptions?: readonly { pillId: string; quantity: number }[];
```

`PrismaExpeditionRepository.start` — sau blok reserve vé, trước `expedition.create`:
```ts
        for (const item of input.loadoutConsumptions ?? []) {
          const decremented = await tx.inventoryItem.updateMany({
            where: { userId: input.userId, pillId: item.pillId, quantity: { gte: item.quantity } },
            data: { quantity: { decrement: item.quantity } },
          });
          if (decremented.count !== 1) throw new DomainError('INSUFFICIENT_INVENTORY', `Không đủ đan trong kho: ${item.pillId}`);
        }
```

`StartExpeditionUseCase`: constructor thêm `private readonly pills: PillRepository` (trước `random`). Flow sau khi lấy `state`:
```ts
    expeditionStartGate(bundle.branch, state.realmMajor, state.realmName);
    const loadout = input.loadoutPillIds && input.loadoutPillIds.length > 0
      ? validateLoadout(await this.pills.listByIds(input.loadoutPillIds))
      : [];
```
(`PillRepository.listByIds(ids: string[]): Promise<PillRecord[]>` — thêm vào port + Prisma impl `findMany({ where: { id: { in: ids } } })` + fakes; nếu thiếu id → pills.length < ids.length → throw `LOADOUT_INVALID, 'Đan không tồn tại'` trong use case.) Snapshot: `combatSnapshot = { ..., loadout }` (chỉ thêm khi loadout.length > 0). `simulateExpedition({ ..., pillBuffs: loadout })`. Repo start: `loadoutConsumptions: loadout.map((e) => ({ pillId: e.pillId, quantity: 1 }))`.

Lưu ý `state.realmName` — kiểm `CultivationStateOutput` có `realmName` (FE types show realmName: string ✓ ở cultivation state) — dùng nó.

Cập nhật `app.ts` wiring constructor thêm dep pills repo.

- [ ] **Step 4: Watch pass**: `npx vitest run src/application/expedition.usecases.test.ts src/infrastructure/repositories/expedition.repo.integration.test.ts | grep "Tests "`.

- [ ] **Step 5: Commit** — `"feat: StartExpedition gate realm + loadout tru ton kho (atomic)"`.

---

### Task 9: Routes — schema + errors + branches response

**Files:**
- Modify: `backend/src/presentation/schemas/expedition.schemas.ts`
- Modify: `backend/src/presentation/middleware/errorHandler.ts`
- Test: `backend/tests/integration/expedition.routes.test.ts`

- [ ] **Step 1: Test route (failing)** — thêm vào `expedition.routes.test.ts` (pattern test hiện có):
```ts
it('POST /expeditions/start tầng 2 khi realm thấp → 409 EXPEDITION_REALM_GATE', ...);
it('loadoutPillIds trùng → 400 LOADOUT_INVALID (zod)', ...);
it('loadoutPillIds quá 2 → 400', ...);
it('start khi battlePower < recommendedPower vẫn 200 (soft chiến lực)', ...);
it('GET /expeditions/branches trả tier/minRealmMajor/recommendedPower/bossDropWeights', ...);
```

- [ ] **Step 2: Implement**

`expedition.schemas.ts`:
```ts
export const startExpeditionSchema = z.object({
  branchId: z.string().regex(/^[a-z0-9-]+$/),
  difficulty: z.enum(['easy', 'normal', 'hard']),
  durationSec: z.union([z.literal(1_800), z.literal(7_200), z.literal(28_800)]),
  loadoutPillIds: z.array(z.string().regex(/^[a-z0-9-]+$/)).max(2).refine((ids) => new Set(ids).size === ids.length, 'loadoutPillIds must be distinct').optional(),
});
```

`errorHandler.ts` thêm: `EXPEDITION_REALM_GATE: 409, LOADOUT_INVALID: 400, PILL_NOT_CONSUMABLE: 400`.

- [ ] **Step 3: Watch pass** → commit `"feat: route loadout + realm gate error mapping"`.

---

### Task 10: Admin expedition config validate + zod

**Files:**
- Modify: `backend/src/application/UpdateExpeditionConfigAdminUseCase.ts` (+ `admin.expedition-catalog.test.ts`)
- Modify: `backend/src/presentation/schemas/admin.schemas.ts` (`expeditionBranchSchema`)

- [ ] **Step 1: Test failing**:
```ts
it('replace round-trip tier/minRealmMajor/recommendedPower/bossDropWeights', ...);
it.each([['tier 0', { tier: 0 }], ['tier 4', { tier: 4 }], ['minRealmMajor -1', { minRealmMajor: -1 }], ['recommendedPower âm', { recommendedPower: -1 }]])
  ('INVALID_EXPEDITION_CONFIG khi %s', ...);
it('bossDropWeights weight âm → INVALID_EXPEDITION_CONFIG', ...);
```

- [ ] **Step 2: Implement** — trong use case sau check branch hiện có:
```ts
      if (!Number.isInteger(row.branch.tier) || row.branch.tier < 1 || row.branch.tier > 3) throw new DomainError('INVALID_EXPEDITION_CONFIG', `branch ${row.branch.id}: tier must be 1..3`);
      if (!Number.isInteger(row.branch.minRealmMajor) || row.branch.minRealmMajor < 0 || row.branch.minRealmMajor > 10) throw new DomainError('INVALID_EXPEDITION_CONFIG', `branch ${row.branch.id}: minRealmMajor must be 0..10`);
      if (!(row.branch.recommendedPower >= 0)) throw new DomainError('INVALID_EXPEDITION_CONFIG', `branch ${row.branch.id}: recommendedPower must be >= 0`);
      for (const weight of row.branch.bossDropWeights) {
        if (!weight.materialId || weight.weight < 0) throw new DomainError('INVALID_EXPEDITION_CONFIG', `invalid boss drop weight: ${weight.materialId}`);
      }
```
Zod `expeditionBranchSchema.branch` thêm (default giữ hành vi cũ cho payload chưa gửi):
```ts
    tier: z.number().int().min(1).max(3).default(1),
    minRealmMajor: z.number().int().min(0).max(10).default(0),
    recommendedPower: z.number().min(0).default(0),
    bossDropWeights: z.array(z.object({ materialId: z.string().regex(/^[a-z0-9-]+$/), weight: z.number().min(0) })).default([]),
```
(`upgradeMaterialWeights.min(1)` giữ nguyên.)

- [ ] **Step 3: Watch pass** → commit `"feat: admin expedition config tier/gate/boss drop validate"`.

---

### Task 11: Seed — Đan Hỏa Tủy + boss weights + 3 đan combat + recipes

**Files:** `backend/prisma/seed.ts`

- [ ] **Step 1: Thêm seed (idempotent theo pattern upsert hiện có)**

Sau `MATERIALS_CONGPHAP_T2` block:
```ts
const MATERIALS_PHASE3 = [
  { id: 'dan-hoa-tuy', name: 'Đan Hỏa Tủy', glyph: '髓', rarity: 5, tier: 3, description: 'Tủy hỏa tinh ngưng vạn năm, chỉ rơi từ cường địch tầng cao.' },
] as const;

const COMBAT_PILLS_T2 = [
  { id: 'cuong-the-dan', name: 'Cường Thể Đan', glyph: '強', rarity: 3, effectKind: 'combatBuff', ...NO_STATS, bonusPct: 25, combatAttribute: 'congVatLy', combatTrigger: 'start', desc: 'Tăng 25% Công Vật Lý trong suốt chuyến bí cảnh.', active: true, starterQuantity: 0, tier: 2 },
  { id: 'kim-cang-dan', name: 'Kim Cang Đan', glyph: '剛', rarity: 3, effectKind: 'combatBuff', ...NO_STATS, bonusPct: 30, combatAttribute: 'phongThu', combatTrigger: 'start', desc: 'Tăng 30% Phòng Thủ trong suốt chuyến bí cảnh.', active: true, starterQuantity: 0, tier: 2 },
  { id: 'huyen-huyet-dan', name: 'Huyền Huyết Đan', glyph: '玄', rarity: 4, effectKind: 'combatBuff', ...NO_STATS, bonusPct: 35, combatAttribute: 'khiHuyet', combatTrigger: 'lowHp30', desc: 'Khi Khí Huyết tụt dưới 30%, tăng 35% Khí Huyết (một lần mỗi trận).', active: true, starterQuantity: 0, tier: 2 },
] as const;

const COMBAT_RECIPES_T2 = [
  { id: 'recipe-cuong-the-dan', pillId: 'cuong-the-dan', durationSec: 9600, linhThachCost: 80, tier: 2, minAlchemyRank: 4, baseSuccessPct: 70, ingredients: [['huyet-long-sam', 3], ['huyen-thiet-tam', 2], ['xich-viem-tinh', 2]] },
  { id: 'recipe-kim-cang-dan', pillId: 'kim-cang-dan', durationSec: 9600, linhThachCost: 80, tier: 2, minAlchemyRank: 4, baseSuccessPct: 70, ingredients: [['kim-sa-luc', 3], ['chu-tuoc-vu', 2], ['han-bang-ngoc', 2]] },
  { id: 'recipe-huyen-huyet-dan', pillId: 'huyen-huyet-dan', durationSec: 14400, linhThachCost: 120, tier: 2, minAlchemyRank: 4, baseSuccessPct: 65, ingredients: [['huyet-long-sam', 3], ['hoang-tuyen-thuy', 2], ['long-mach-sa', 2]] },
] as const;

const BOSS_DROPS: Record<string, { biTich: [string, string, string]; danHoaTuy: number }> = {
  'thanh-lam': { biTich: ['bi-tich-vong-coc', 'bi-tich-ngu-kiem', 'bi-tich-dieu-hoa'], danHoaTuy: 0.3 },
  'u-minh': { biTich: ['bi-tich-tieu-chu-thien', 'bi-tich-thien-loi', 'bi-tich-ninh-dan'], danHoaTuy: 0.3 },
  'van-hai': { biTich: ['bi-tich-dai-chu-thien', 'bi-tich-kim-cang', 'bi-tich-van-linh-lo'], danHoaTuy: 0.3 },
  'long-mach': { biTich: ['bi-tich-vong-coc', 'bi-tich-ngu-kiem', 'bi-tich-dieu-hoa'], danHoaTuy: 0.5 },
  'tinh-thien': { biTich: ['bi-tich-tieu-chu-thien', 'bi-tich-thien-loi', 'bi-tich-ninh-dan'], danHoaTuy: 0.5 },
};
```

Loop seed (sau blok tier2 drops hiện có, trong cùng `for (const branch of BRANCHES)`):
```ts
    const boss = BOSS_DROPS[branch.id];
    if (boss) {
      for (const materialId of boss.biTich) {
        await prisma.expeditionBossDropWeight.upsert({
          where: { branchId_materialId: { branchId: branch.id, materialId } },
          create: { branchId: branch.id, materialId, weight: 1 },
          update: { weight: 1 },
        });
      }
      await prisma.expeditionBossDropWeight.upsert({
        where: { branchId_materialId: { branchId: branch.id, materialId: 'dan-hoa-tuy' } },
        create: { branchId: branch.id, materialId: 'dan-hoa-tuy', weight: boss.danHoaTuy },
        update: { weight: boss.danHoaTuy },
      });
    }
```
Materials: `for (const m of [...MATERIALS_CONGPHAP_T2, ...MATERIALS_PHASE3])` (mở rộng loop hiện có). Pills: `for (const p of [...PILLS, ...PILLS_T2, ...COMBAT_PILLS_T2])` — cần thêm combatAttribute/combatTrigger vào pill upsert create/update (spread NO_STATS đã có null cho 4 stat cũ; thêm 2 field vào create+update map — kiểm tra code upsert pill hiện tại: nếu build object thủ công thì thêm `combatAttribute: p.combatAttribute ?? null, combatTrigger: p.combatTrigger ?? null`). Recipes: `[...RECIPES, ...RECIPES_T2, ...COMBAT_RECIPES_T2]`.

Kiểm `NO_STATS` hiện có gồm `combatAttribute`/`combatTrigger` chưa — nếu chưa, cập nhật NO_STATS (cho PILLS/PILLS_T2 đều null 2 field combat) và bỏ `...NO_STATS` khỏi COMBAT (vì cần bonusPct riêng) — **quyết định**: NO_STATS mới = `{ amount: null, multiplier: null, durationSec: null, bonusPct: null, combatAttribute: null, combatTrigger: null }`; COMBAT_PILLS_T2 giữ spread rồi ghi đè bonusPct/combatAttribute/combatTrigger.

- [ ] **Step 2: Chạy seed 2 lần (idempotent)** + verify DB:
```bash
cd backend && npm run db:seed && npm run db:seed 2>&1 | tail -1
docker exec backend-db-1 psql -U game -d tu_tien_chi_lo -c "SELECT count(*) FROM \"ExpeditionBossDropWeight\"; SELECT \"effectKind\", count(*) FROM \"Pill\" GROUP BY 1; SELECT id, tier, \"minRealmMajor\" FROM \"ExpeditionBranch\" ORDER BY \"basePower\";"
```
Expected: 17 boss weights (5 nhánh × 3 bi-tich + 5... = 15 bi-tich + 5 dan-hoa-tuy = 20), combatBuff=3, tier 2/3 nhánh đúng.

- [ ] **Step 3: Commit** — `"seed: dan hoa tuy + boss drop bi tich + 3 dan combat T2"`.

---

### Task 12: FE player — types/api/tier groups/lock/warning + loadout UI + pill guard

**Files:**
- Modify: `frontend/src/lib/types.ts` (`ExpeditionBranchConfigDTO` +4 field mới; `StartExpeditionInput` + `loadoutPillIds?: string[]`; `PillEffectKind` + `combatBuff`; `PillDTO` +2 field nếu có)
- Modify: `frontend/src/lib/pill-constants.ts` (label effectKind combatBuff)
- Modify: `frontend/src/lib/pill-validation.ts` (+ test `pill-validation.test.ts`) — mirror combatBuff rules
- Modify: `frontend/src/components/expedition-drawer.tsx` (+ CSS `frontend/src/app/globals.css`)
- Modify: kho đan (`frontend/src/components/*inventory*` hoặc pills drawer — guard nút "Dùng" với combatBuff, nhãn "Đan bí cảnh")

- [ ] **Step 1: Types/api trước** — chạy `pnpm exec tsc --noEmit` sạch.

- [ ] **Step 2: Pill validation mirror (TDD)** — thêm test trong `pill-validation.test.ts` (mirror Task 6) rồi implement.

- [ ] **Step 3: expedition-drawer**:
  - Group branches theo `tier` (1→3): header `Tầng N · Yêu cầu cảnh giới {getRealmMeta(minRealmMajor).name}`; sort theo tier rồi basePower.
  - Nhánh realm chưa đạt: mờ + disable nút start + `<small className="alchemy-recipe-lock">Cần đạt cảnh giới {name}</small>`.
  - Soft warning: `battlePower < recommendedPower` → dòng vàng `Chiến lực khuyến nghị {recommendedPower} (hiện tại {battlePower})` — vẫn cho start.
  - 2 slot loadout: state `loadout: (string | null)[]` = [null, null]; nút "＋" mở modal chọn từ kho (chỉ pill `effectKind === 'combatBuff'`, quantity > 0, không trùng slot kia); chip hiển thị đan đã chọn + nút bỏ; gửi `loadoutPillIds: loadout.filter(Boolean)`.
  - Props/context cần: `battlePower` + `realmMajor` (từ cultivation state — kiểm drawer hiện nhận prop gì; thêm nếu thiếu).

- [ ] **Step 4: Kho đan guard** — nút Dùng disable với `combatBuff` + tooltip "Chỉ dùng trong loadout bí cảnh"; nhãn loại đan cập nhật `pill-constants.ts` label map.

- [ ] **Step 5:** `pnpm vitest run src` + `pnpm lint` (chú ý `biome check --write` trước khi lint nếu format lệch) + `pnpm exec tsc --noEmit`.

- [ ] **Step 6: Commit** — `"feat(fe): tang bi canh + loadout 2 slot dan combat + guard dan combatBuff"`.

---

### Task 13: FE admin — expeditions config + pills combatBuff

**Files:**
- Modify: `frontend/src/app/admin/expeditions/page.tsx` (form branch + bossDropWeights editor)
- Modify: `frontend/src/app/admin/pills/page.tsx` (kind combatBuff + 2 field select)

- [ ] **Step 1: Expeditions page** — branch form thêm: `tier` (select 1..3), `minRealmMajor` (number, gợi ý theo tier {1→0,2→3,3→5}), `recommendedPower` (number), section `bossDropWeights` (danh sách materialId+weight, pattern giống upgradeMaterialWeights editor hiện có trong cùng file). Round-trip từ fetch → state → body PUT.

- [ ] **Step 2: Pills page** — select effectKind thêm `combatBuff`; khi chọn: disable/khóa amount/multiplier/durationSec (giữ bonusPct), hiện select `combatAttribute` (6 thuộc tính từ `ATTRIBUTE_LABELS` có sẵn) + `combatTrigger` (start = "Kích ngay khi vào trận" / lowHp30 = "Khi Khí Huyết ≤ 30%"). Chuyển kind khác → reset 2 field về null.

- [ ] **Step 3:** lint + tsc + build (`pnpm build`).

- [ ] **Step 4: Commit** — `"feat(fe-admin): expedition tier/boss drop + pill combatBuff form"`.

---

### Task 14: Nghiệm thu + docs

- [ ] **Step 1: Đối chiếu 10 tiêu chí §8 spec** (ghi evidence ngắn trong message cuối):
  1. Migration additive + status sạch + seed idempotent ✓ (Task 1/11)
  2. Hard gate realm (Task 9 route test)
  3. Soft chiến lực vẫn start (Task 9)
  4. Loadout: trừ tồn kho atomic / lỗi đúng mã (Task 7+8+9)
  5. start trigger lượt 1 (Task 5 test)
  6. lowHp30 một lần/battle, reset battle sau (Task 5 test)
  7. Boss drop bi-tich → MaterialInventory sau claim (thêm 1 integration case ở `expedition.routes.test.ts`: start nhánh tầng 2 với random seed ép boss thắng? — *đơn giản hơn*: unit test rollExpeditionRewards (Task 7) + claim repo integration đã cover grant materials chung; thêm case trực tiếp claim với reward chứa bi-tich → MaterialInventory có bi-tich đó)
  8. Regression suite cũ pass (cuối)
  9. Admin round-trip (Task 10) + pill combatBuff admin (Task 6)
  10. Suites xanh: `cd backend && npm test` + `npm run build`; `cd frontend && pnpm test && pnpm lint && pnpm exec tsc --noEmit && pnpm build`

- [ ] **Step 2: Cập nhật `README.md` + `CLAUDE.md`** (section mới "Bí Cảnh 2.0 (Tam hệ Liên hoàn — Phase 3)" mirror style Phase 2; cập nhật test counts; bảng endpoint thêm dòng loadout).

- [ ] **Step 3: Commit** — `"test+docs: nghiem thu Phase 3 Bi Canh 2.0"`.

---

## Self-review notes (đã kiểm)

- Mọi type/name trong task sau khớp task trước: `LoadoutEntry`, `CombatBuffTrigger`, `bossDropWeights`, `expeditionStartGate`, `validateLoadout`, `loadoutConsumptions`, `PILL_NOT_CONSUMABLE`.
- **Thứ tự thực thi chính xác**: 1 → 2 → 3 → **6** → 4 → 5 → 7 → 8 → 9 → 10 → 11 → 12 → 13 → 14 (Task 6 pill kind phải trước Task 4 validateLoadout vì fixture PillRecord cần field/kind mới).
- Circular import: `combat.calc.ts` chỉ `import type { LoadoutEntry }` từ `expedition/expedition.ts` (expedition.ts không import combat.calc) — an toàn.
- Fixture cũ dựng `ExpeditionBranchConfig` phải thêm 4 field (Task 1 Step 4) và `PillRecord` thêm 2 field (Task 6 Step 3) — tsconfig không typecheck `backend/tests/` nhưng các file test colocate trong `src/` thì có.
