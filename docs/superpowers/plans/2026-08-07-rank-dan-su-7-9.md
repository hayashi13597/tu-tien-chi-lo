# Rank Đan Sư 7–9 Implementation Plan

**Goal:** Mở rank Đan Sư 7–9 gate Hóa Thần (realmMajor 5) + 1 Đan Hỏa Tủy/cấp, cost ĐK 3100/4300/5700.

**Spec:** `docs/superpowers/specs/2026-08-07-rank-dan-su-7-9-design.md`

**Lệnh chuẩn:** backend `cd backend && npx vitest run <file>` (nhanh) / `npm test` (full); frontend `pnpm vitest run src`, `pnpm lint`, `pnpm exec tsc --noEmit`, `pnpm build`.

---

### Task 1: Domain `alchemy.profile.ts` — hằng + rankUpCheck mở rộng

**Files:** `backend/src/domain/alchemy/alchemy.profile.ts` + `alchemy.profile.test.ts`

- [ ] **Step 1: Test failing** — thêm vào `alchemy.profile.test.ts` (xem style hiện có):

```ts
describe('rankUpCheck rank 7-9 (Thiên Giai)', () => {
  const base = { rank: 6, danKhi: 10_000 };
  it('rank 7 hợp lệ: đủ realm 5 + ĐK 3100 + Tủy 1', () => {
    expect(() => rankUpCheck(base, 7, 5, 1)).not.toThrow();
  });
  it('rank 9 hợp lệ ở realm 5 (cùng gate Hóa Thần)', () => {
    expect(() => rankUpCheck({ rank: 8, danKhi: 9999 }, 9, 5, 1)).not.toThrow();
  });
  it('rank 10 → ALCHEMY_RANK_LOCKED (cap mới 9)', () => {
    expect(() => rankUpCheck({ rank: 9, danKhi: 99999 }, 10, 9, 9)).toThrowError(/ALCHEMY_RANK_LOCKED|tối đa/);
  });
  it('rank 7 realm 4 → ALCHEMY_REALM_GATE', () => {
    expect(() => rankUpCheck(base, 7, 4, 1)).toThrowError(/realmMajor 5/);
  });
  it('thiếu Đan Khí → INSUFFICIENT_DAN_KHI (message mang cost 3100)', () => {
    expect(() => rankUpCheck({ rank: 6, danKhi: 100 }, 7, 5, 1)).toThrowError(/3100/);
  });
  it('đủ ĐK + realm nhưng 0 Tủy → ALCHEMY_MISSING_DAN_HOA_TUY', () => {
    try {
      rankUpCheck(base, 7, 5, 0);
      expect.unreachable();
    } catch (e) {
      expect((e as DomainError).code).toBe('ALCHEMY_MISSING_DAN_HOA_TUY');
    }
  });
  it('rank 1-6 không cần Tủy (hành vi cũ)', () => {
    expect(() => rankUpCheck({ rank: 5, danKhi: 2100 }, 6, 0)).not.toThrow();
  });
});
```

- [ ] **Step 2: Watch fail** — `npx vitest run src/domain/alchemy/alchemy.profile.test.ts → các case 7-9 fail.

- [ ] **Step 3: Implement**
```ts
// Phase 3 mở tới cấp 9 (Thiên Giai); cấp 7-9 yêu cầu Đan Hỏa Tủy (boss tầng 2+).
export const MAX_RANK = 9;

export const RANK_UP_COSTS: Readonly<Record<number, number>> = {
  2: 100, 3: 300, 4: 700, 5: 1300, 6: 2100,
  // Thiên Giai: tiếp quy luật diff +200 (+1000/+1200/+1400).
  7: 3100, 8: 4300, 9: 5700,
};
// realmMajor tối thiểu để lên cấp tương ứng (Kết Đan = 3, Hóa Thần = 5).
export const RANK_REALM_GATES: Readonly<Record<number, number>> = { 4: 3, 7: 5, 8: 5, 9: 5 };
// Đan Hỏa Tủy cần cho từng cấp Thiên Giai (Phase 3: 1 quả mỗi cấp).
export const RANK_DAN_HOA_TUY_COSTS: Readonly<Record<number, number>> = { 7: 1, 8: 1, 9: 1 };
```

`rankUpCheck(profile, targetRank, realmMajor = 0, danHoaTuyOwned = 0)`:
```ts
  if (targetRank > MAX_RANK) {
    throw new DomainError('ALCHEMY_RANK_LOCKED', `Đan Sư đã đạt cấp tối đa ${MAX_RANK}`);
  }
  const gateRealm = RANK_REALM_GATES[targetRank];
  if (gateRealm !== undefined && realmMajor < gateRealm) {
    throw new DomainError('ALCHEMY_REALM_GATE', `cần cảnh giới tối thiểu realmMajor ${gateRealm} để lên Đan Sư cấp ${targetRank}`);
  }
  const cost = RANK_UP_COSTS[targetRank];
  ... (giữ INSUFFICIENT_DAN_KHI)
  const tuyCost = RANK_DAN_HOA_TUY_COSTS[targetRank] ?? 0;
  if (tuyCost > 0 && danHoaTuyOwned < tuyCost) {
    throw new DomainError('ALCHEMY_MISSING_DAN_HOA_TUY', `cần ${tuyCost} Đan Hỏa Tủy để lên Đan Sư cấp ${targetRank} — rơi từ boss bí cảnh tầng 2+`);
  }
```

- [ ] **Step 4: Watch pass** — cả file (hành vi 1-6 giữ nguyên).

- [ ] **Step 5: Commit** — `"feat: rankUpCheck rank 7-9 (Hoa Than gate + Dan Hoa Tuy)"`.

---

### Task 2: Repo `rankUp` serializable trừ Tủy

**Files:** `backend/src/domain/ports/AlchemyRepository.ts`, `backend/src/infrastructure/repositories/PrismaAlchemyRepository.ts`, `backend/src/infrastructure/repositories/alchemy.repo.integration.test.ts`

- [ ] **Step 1: Test failing** — thêm case trong `alchemy.repo.integration.test.ts`:
```ts
it('rankUp 6→7 trừ ĐK + 1 Đan Hỏa Tủy; thiếu Tủy → rollback', async () => {
  // tạo user+character+profile {rank:6, danKhi: 3100}; grant MaterialInventory dan-hoa-tuy = 1
  await repo.rankUp(userId, 7, 3100, 1); // → profile rank 7, ĐK 0, Tủy 0
  // set lại: grant tủy 0, đủ ĐK: rankUp(7→8) → ALCHEMY_MISSING_DAN_HOA_TUY, ĐK không trừ
});
```
(xem helper tạo user/profile có sẵn trong file)

- [ ] **Step 2: Watch fail** (port chưa có tham số → TS lỗi / runtime không trừ).

- [ ] **Step 3: Implement**
- Port: `rankUp(userId: string, targetRank: number, danKhiCost: number, danHoaTuyCost?: number): Promise<AlchemyProfileRecord>;`
- Prisma impl:
```ts
  async rankUp(userId: string, targetRank: number, danKhiCost: number, danHoaTuyCost = 0): Promise<AlchemyProfileRecord> {
    try {
      return await this.client.$transaction(async (tx) => {
        const updated = await tx.alchemyProfile.updateMany({
          where: { userId, rank: targetRank - 1, danKhi: { gte: danKhiCost } },
          data: { rank: targetRank, danKhi: { decrement: danKhiCost } },
        });
        if (updated.count !== 1) throw new DomainError('INSUFFICIENT_DAN_KHI', 'không đủ Đan Khí hoặc sai cấp hiện tại');
        if (danHoaTuyCost > 0) {
          const tuy = await tx.materialInventory.updateMany({
            where: { userId, materialId: 'dan-hoa-tuy', quantity: { gte: danHoaTuyCost } },
            data: { quantity: { decrement: danHoaTuyCost } },
          });
          if (tuy.count !== 1) throw new DomainError('ALCHEMY_MISSING_DAN_HOA_TUY', 'không đủ Đan Hỏa Tủy');
        }
        const profile = await tx.alchemyProfile.findUniqueOrThrow({ where: { userId } });
        return { id: profile.id, userId: profile.userId, characterId: profile.characterId, rank: profile.rank, danKhi: profile.danKhi, furnaceLevel: profile.furnaceLevel };
      }, { isolationLevel: Prisma.TransactionIsolationLevel.Serializable });
    } catch (error) {
      if (error instanceof Prisma.PrismaClientKnownRequestError && error.code === 'P2034') {
        throw new DomainError('CONCURRENT_MODIFICATION', 'Hồ sơ Đan Sư vừa được thay đổi bởi request khác');
      }
      throw error;
    }
  }
```

- [ ] **Step 4: Watch pass.**

- [ ] **Step 5: Commit** — `"feat: rankUp tru Dan Hoa Tuy atomic (tx serializable)"`.

---

### Task 3: Use case + profile summary + error mapping

**Files:** `backend/src/application/RankUpAlchemyUseCase.ts`, `GetAlchemyProfileUseCase.ts`, `backend/src/presentation/middleware/errorHandler.ts`, `backend/src/app.ts` (wiring) + tests unit 2 file đó (`backend/tests/unit/` hoặc colocate — kiểm `src/application/alchemy.profile.usecases.test.ts`).

- [ ] **Step 1: Test failing**
  - RankUp: fake profile rank 6, character realmMajor 5, materials dan-hoa-tuy 0 → execute → error code `ALCHEMY_MISSING_DAN_HOA_TUY`; owned 1 → repo.rankUp gọi với (userId, 7, 3100, 1).
  - GetAlchemyProfile: profile rank 6 + tủy 0 → `nextRank` chứa `{ target: 7, danKhiCost: 3100, realmGateMajor: 5, danHoaTuyCost: 1, danHoaTuyOwned: 0, affordableDanHoaTuy: false }`; profile rank 9 → `nextRank: null`.

- [ ] **Step 2: Implement**
  - `RankUpAlchemyUseCase` constructor thêm `materials: MaterialRepository`; trong execute:
```ts
    const profile = await this.alchemy.getProfile(userId);
    const character = await this.characters.findByUserId(userId);
    if (!character) throw new DomainError('CHARACTER_NOT_FOUND', `character not found for user: ${userId}`);
    const targetRank = profile.rank + 1;
    const tuyOwned = (await this.materials.getQuantity(userId, 'dan-hoa-tuy')) ?? 0;
    rankUpCheck(profile, targetRank, character.realmMajor, tuyOwned);
    return this.alchemy.rankUp(userId, targetRank, RANK_UP_COSTS[targetRank] ?? 0, RANK_DAN_HOA_TUY_COSTS[targetRank] ?? 0);
```
    (Kiểm `MaterialRepository` có `getQuantity`; nếu không có thì `getInventory(userId)`/`find` — dùng API hiện có, ví dụ `listByUser` rồi `.find`; giữ đơn giản: `const inv = await this.materials.listByUser(userId); const tuyOwned = inv.find(i => i.materialId === 'dan-hoa-tuy')?.quantity ?? 0`.)
  - `GetAlchemyProfileUseCase`: +dep `MaterialRepository`; `nextRank` thêm:
```ts
      danHoaTuyCost: RANK_DAN_HOA_TUY_COSTS[nextTarget] ?? 0,
      danHoaTuyOwned: tuyOwned,
      affordableDanHoaTuy: tuyOwned >= (RANK_DAN_HOA_TUY_COSTS[nextTarget] ?? 0),
```
  - `errorHandler`: `ALCHEMY_MISSING_DAN_HOA_TUY: 409`.
  - `app.ts`: truyền `materialRepository` vào 2 use case (và sửa mọi test khởi tạo 2 use case này — tsc chỉ ra).

- [ ] **Step 3: Watch pass** → commit `"feat: rankUp use case +GetAlchemyProfile Dan Hoa Tuy fields"`.

---

### Task 4: Route integration

**Files:** tìm file route test alchemy (`tests/integration/alchemy*` — hiện có `alchemy.profile.routes.test.ts` hoặc trong `materials.alchemy.routes.test.ts`).

- [ ] **Step 1+2: Test + verify**: user mới, SQL set profile `{rank:6, danKhi: 3100}`, character realmMajor 5 (update qua prisma), không Tủy → POST /alchemy/rank-up → 409 `ALCHEMY_MISSING_DAN_HOA_TUY`; grant Tủy 1 → 200, profile rank 7, Tủy 0, ĐK 0. Realm thấp (4) → 409 `ALCHEMY_REALM_GATE`.

- [ ] **Step 3: Commit** — `"test: route rank-up 7 Dan Hoa Tuy (3 case)"`.

---

### Task 5: FE

**Files:** `frontend/src/lib/types.ts` (`AlchemyProfileDTO.nextRank` +3 field), `frontend/src/components/alchemy-drawer.tsx` (nút/hint), CSS nếu cần + `frontend/src/lib/alchemy-constants.ts`(nếu có MAX_RANK ở FE — grep "MAX_RANK\|cấp 7" FE).

- [ ] **Step 1:** Types + tìm đoạn FE đang nói "cấp 7-9 mở phase sau" (grep `9\|7–9\|phase sau`) → cập nhật cap 9.
- [ ] **Step 2:** Drawer: nút rank-up rendering cost khi `nextRank.danHoaTuyCost > 0`: hiện `ĐK {danKhiCost} + Đan Hỏa Tủy ×1 (có {danHoaTuyOwned})`; disable khi `!affordableDanHoaTuy` kèm `<small>` hint rơi từ boss tầng 2+ (tái dùng class `alchemy-recipe-lock`).
- [ ] **Step 3:** `pnpm vitest run src` + lint + tsc + build.
- [ ] **Step 4: Commit** — `"feat(fe): rank 7-9 cost Dan Hoa Tuy o alchemy drawer"`.

---

### Task 6: Nghiệm thu + docs

- [ ] **Step 1:** Đối chiếu 6 tiêu chí §3 spec; full verify: backend `npm test` + `npm run build`; frontend `pnpm test && pnpm lint && pnpm exec tsc --noEmit && pnpm build`.
- [ ] **Step 2:** Cập nhật `README.md` (test counts) + `CLAUDE.md` (section rank 7-9 thêm vào cuối mục Luyện Đan 2.0, fix ghi chú MAX_RANK 6).
- [ ] **Step 3: Commit** `"test+docs: nghiem thu rank Dan Su 7-9"`.
